# 「이 회차 갈까말까」 design.md

> **문서 범위**
> 화면 구조, 씬 구성, 아트 디렉션, 애니메이션, 타이포그래피, 에셋 목록을 규정한다.
> **판단 로직·에이전트 명세·데이터 구조는 `prd.md`에 있으며 여기서 반복하지 않는다.**
> PRD가 정한 데이터 계약(푯말 16자, detail 100~180자, `is_muted`, 축 점수 -2~+2)을 제약 조건으로 받는다.

| 항목 | 내용 |
|---|---|
| 버전 | v1.1 (2026-08-25 — 상담실 아이소메트릭 리디자인 반영) |
| 작성일 | 2026-08-16 |
| 아트 스타일 | **랜딩은 픽셀 아트(도트), 상담실은 일러스트 PNG 기반 골목 씬** — 두 화면이 더 이상 같은 아트 스타일을 쓰지 않는다. 상세는 §2.4a·§4 |
| 화면 수 | **2** (랜딩 `index.html` / 상담실 `consult.html`) |
| 부속 문서 | `prd.md`, `test_cases.md` |

> **v1.1 변경 요지.** 상담실을 "부스에 앉은 도트 캐릭터"에서 "보도블록 골목에 늘어선 건물"로 다시 그리면서 도트체·픽셀 스프라이트·흔들림 애니메이션을 걷어냈다. 랜딩은 원안 그대로다. 이 리비전은 문서를 실제 코드에 맞춰 갱신한 것이며, 바뀐 절 옆에 **[v1.1]** 표시를 붙인다.

---

## 1. 디자인 원칙

### 원칙 1 — 조명이 전체를 관통하는 언어다

밝기 변화가 이 서비스의 유일한 상태 표현 수단이다. 별도의 프로그레스 바나 로딩 스피너를 쓰지 않는다.

```
로그인 전   폼 영역 어두움
로그인      조명이 켜지며 폼 활성화
입력 진행   오른쪽 좌석 스포트라이트가 점점 밝고 넓어짐
입력 완료   조명이 완전히 들어오며 메인으로 전환
```

공연장의 조명 은유가 뮤지컬 서비스라는 정체성과 직결되며, 어떤 진행률 표시보다 맥락에 맞는다.

### 원칙 2 — 색과 굵기가 정보다 [v1.1]

> 원안은 "푯말이 흔들리는 진폭·속도로 강도를 전달한다"였다. 구현 단계에서 흔들림 애니메이션은 산만하다는 판단으로 빠졌고, 같은 정보를 **정지된 두 가지 시각 신호**로 옮겼다 — 상담실에만 해당하며, §5.2에 실측 스펙이 있다.

사용자가 말풍선 텍스트를 읽기 **전에** 두 가지만 보고 "오늘은 두 명이 강하게 말하고 있구나"를 파악할 수 있어야 한다.

```
부호(+/0/-)  → 말풍선 테두리·꼬리 색 + 신호등(빨강·주황·초록)
세기(|axis_score|) → 말풍선 테두리 굵기 (2px → 3px → 4px)
```

`is_muted`는 말풍선 자체가 뜨지 않는다(원칙 유지, §5.1).

### 원칙 3 — 도트체는 캐릭터가 말할 때만 (랜딩 한정) [v1.1]

> 상담실은 도트체를 전부 걷어내고 본문 서체(Pretendard) 하나로 통일했다. 아래 표는 **랜딩(`index.html`)에만** 적용된다.

| 폰트 | 쓰는 곳 (랜딩) |
|---|---|
| **도트체 (갈무리)** | 폼 버튼, 제목, 「상담 시작」 |
| **일반 폰트 (Pretendard)** | 입력 폼, 공연 정보, 금액·좌석 번호 |

기준은 **"캐릭터가 말하는 것"** vs **"시스템이 보여주는 정보"**였으나, 상담실에서는 도트체로 말하는 캐릭터 자체가 없어졌다(§2.4a) — 푯말·말풍선·결론·버튼 전부 Pretendard. 공연 제목·배우 이름·좌석 번호·금액은 랜딩에서도 정확히 읽혀야 하므로 도트체를 쓰지 않는다.

### 원칙 4 — 5개 푯말이 화면의 주인공이다

PRD §1.3에 따라 결론보다 푯말이 핵심 가치다. 결론은 단어 하나로 크게 보여주되, 화면 면적과 시선은 푯말이 가져간다.

### 원칙 5 — 무료·상업 이용 가능한 에셋만 사용한다

폰트·아이콘·사운드 등 모든 외부 에셋은 상업적 이용이 명시적으로 허용된 것만 쓴다(§10).

---

## 2. 아트 디렉션

### 2.1 톤

**중간 회색조 기반 — 랜딩(`index.html`) 한정.** 너무 어둡지도 밝지도 않으며, 조명 영역만 확실히 밝다.

| 이유 | |
|---|---|
| 조명 대비 확보 | 어두운 배경이면 조명 연출이 살지만 긴 본문 가독성이 떨어짐 |
| 본문 가독성 | 입력 폼 레이블·값을 오래 읽어야 함 |

**상담실(`consult.html`)은 다른 톤을 쓴다. [v1.1]** 골목이 노을 지는 저녁으로 설정되어 있어 회색조가 아니라 보라~주황 그라디언트(`--sky-top`~`--sky-low`)를 배경으로 깐다. §2.4a 참고. 두 화면이 같은 캐릭터 스프라이트를 공유한다는 전제가 사라졌으므로("캐릭터를 두 벌 만들지 않는다") 톤을 억지로 맞추지 않는다.

### 2.2 컬러 팔레트

픽셀 아트는 색 수를 제한할수록 완성도가 올라간다. 아래 표는 **`tokens.css`가 두 화면에 공유하는 16색**이며, 실제 코드와 정확히 일치한다. **16색 제한은 픽셀 아트 화면(랜딩)에 대한 규칙이다** — 일러스트 배경을 쓰는 상담실은 이 표의 신호색(강조·긍정·부정·중립·푯말 테두리)만 공유하고, 배경·지면 색은 §2.4a의 별도 팔레트를 쓴다(제한 밖).

```
배경        #3A3A3A  (중간 회색, 기본)
배경 어두움  #2A2A2A  (비활성 영역)
배경 밝음    #4A4A4A  (부스 내부)

조명        #F5E6C8  (스포트라이트, 따뜻한 백색)
조명 약     #8A7F6A  (조명 감쇠 구간)

텍스트 주   #F0F0F0
텍스트 보조  #A0A0A0
텍스트 비활성 #6A6A6A

강조        #E5A93C  (팀장 결론, 강한 신호. 기존 이미지의 노란 포인트)
긍정        #7FB069  (+ 축 신호)
부정        #C4674F  (- 축 신호)
중립        #8A8A8A  (0 축 신호)

푯말 바탕   #E8DFC8  (기존 이미지의 나무 푯말 톤)
푯말 테두리  #6B5842
```

**축 점수 색은 말풍선 테두리·꼬리와 신호등에만 쓴다.** 말풍선 바탕 전체를 색으로 칠하면 화면이 신호등처럼 보이고, 무엇보다 사용자에게 "이건 좋은 것/나쁜 것"을 강하게 주입하게 되어 PRD 원칙 1(어떤 에이전트도 가라/가지마라를 말하지 않는다)에 어긋난다.

### 2.3 스프라이트 규격 (랜딩 한정) [v1.1]

아래 규격은 **랜딩에 남아 있는 픽셀 요소(좌석·아이콘·버튼)에만** 적용된다. 상담실의 에이전트 표현은 §2.4a의 일러스트 규격으로 완전히 대체됐다.

| 항목 | 값 |
|---|---|
| 선호 좌석 아이콘 | 5 × 5 도트 패턴 |
| 극장 좌석 | 26 × 34 px |
| 픽셀 스케일 | 정수배만 (×2, ×3, ×4). 소수배 금지 |
| 렌더링 | `image-rendering: pixelated` 필수 (`tokens.css`가 `img`에 전역 적용) |

**정수배 스케일 원칙이 랜딩의 반응형 설계를 지배한다.** 브레이크포인트마다 ×2 / ×3 / ×4로 전환하며, 중간 크기는 여백으로 흡수한다. 상담실은 `clamp()` 기반 유동 크기(§4.5)를 쓰므로 이 원칙 밖이다.

### 2.4 캐릭터 — 원안 (폐기, 참고용) [v1.1]

> 이 절은 상담실을 "부스에 앉은 도트 캐릭터"로 만들 때의 원안이다. **현재 구현은 §2.4a를 따른다.** 캐릭터 단위로 프레임을 찍는 방식이 실제로는 진행되지 않았고, 대신 AI 이미지 생성으로 에이전트별 "가게 건물" 한 장을 통째로 뽑는 방식으로 바뀌었기 때문에 아래 내용은 더 이상 유효하지 않다. 재활용 가능한 아이디어(관점 에이전트는 소품으로 구분, 팀장은 별도 취급)만 §2.4a에 이어졌다.

<details>
<summary>원안 전문</summary>

기존 제작 에셋을 기준으로 한다.

- **팀장** — 헤드셋 착용. 하단 바 중앙
- **관점 에이전트 4인** — 헤드셋 없음. 상단 2×2 그리드
- 4인은 동일 베이스에 **소품과 색 변주**로 구분한다. 완전히 다른 캐릭터를 5개 그리면 픽셀 아트 물량이 감당되지 않고 일관성도 깨진다

| 에이전트 | 구분 요소 |
|---|---|
| 덕심 | 캐스팅보드, 배우 프로필 시트 |
| 시야 | 좌석배치도, 오페라글라스 |
| 비용 | 계산기, 티켓, 영수증 |
| 이벤트 | 증정품 상자, 폴라로이드 |
| 팀장 | 헤드셋, 안내데스크 |

</details>

### 2.4a 상담실 — 건물 일러스트 [v1.1]

상담실의 아트 방향이 "부스에 앉은 캐릭터"에서 "골목에 늘어선 건물"로 바뀌었다. PRD가 축을 5개(덕심·시야·비용·이벤트·**컨디션**)로 확정하면서(PRD §3.5) 팀장을 포함해 6개체를 한 베이스 캐릭터의 소품 변주로 감당하기 어려워진 것이 계기다. 대신 **에이전트 1명당 상반신 인물 + 그 인물이 운영하는 가게 건물을 한 장의 PNG 일러스트**로 뽑는다 — 소품으로 구분한다는 원안의 취지는 유지하되, 소품이 캐릭터 옆이 아니라 건물 간판·진열대에 그려진다.

| 에이전트 | 건물 컨셉 | 인물이 든 소품 |
|---|---|---|
| 덕심 | MD SHOP·캐스팅보드가 걸린 굿즈샵 | 하트 배지, 굿즈 |
| 시야 | "시야 분석 센터" — 좌석별 시야 확인판 | 쌍안경 |
| 비용 | "티켓 가격 분석소" — 금고·시세표 | 계산기 |
| 이벤트 | "이벤트 연구소" — 증정 목록·도장판 | 카메라 |
| 컨디션 | *(에셋 미제작 — §11 참고)* | — |
| 팀장 | *(에셋 미제작 — §11 참고)* | — |

**에셋 규격.** 560 × 420px (4:3), 배경 투명 PNG, `object-fit:contain`으로 칸에 맞춘다. `image-rendering:auto`로 렌더링해 픽셀레이션을 켜지 않는다(§2.3과 반대) — 일러스트라 뭉개면 오히려 지저분해진다.

**상태 표현은 조명 은유(원칙 1)를 그대로 따르되 수단이 바뀌었다.** 프레임 애니메이션(앉다/서다/타이핑) 대신:
- 미완료 → 완료: 타일 속에 눌려 있던 건물이 `scale(.4)→scale(1)`로 솟아오르며 밝기 회복 (`filter: brightness(0.82)→1`)
- `is_muted`: 계속 어둡게 눌린 채 (`brightness(0.62)`), 솟아오르지 않음
- 진행 중: 보도블록 타일이 `paveIdle` 애니메이션으로 은은하게 깜빡임 (건물 자체는 정지)

**골목 팔레트 (consult.html 로컬, tokens.css 밖 — §2.2 참고).**
```
하늘 위     #4A3A57      하늘 중간   #6E4E56      하늘 아래   #93694C
원경 실루엣(먼)  #3A2E48   원경 실루엣(가까운) #48394C
길          #55483C      연석        #7A6448      가로등 빛   rgba(255,196,120,.28)
보도블록 윗면 #9C9C9C     보도블록 왼쪽 #7C7C7C     보도블록 오른쪽 #5A5A5A
```

---

## 3. 랜딩 페이지

### 3.1 레이아웃

```
┌─────────────────────────┬─────────────────────────┐
│ [로그인]  또는  [이용내역] │                         │
│  ← 상단에 작게            │      이 회차             │
├─────────────────────────┤      갈까말까            │
│                         │                         │
│                         │   다섯 명이 각자의 관점에서 │
│      사전질문 폼          │   이 회차를 판단합니다     │
│      (왼쪽 전체)          │                         │
│                         │  ▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁      │
│                         │  ┃┃┃┃┃┃┃┃┃┃┃┃┃┃      │
│                         │      좌석 한 열           │
│                         │    (스포트라이트)         │
│              [상담 시작]  │                         │
└─────────────────────────┴─────────────────────────┘
```

**로그인은 상단에 작게.** 왼쪽 절반을 차지하지 않는다. 로그인 후에는 같은 자리에 **이용 내역 버튼**만 남고, 누르면 모달로 과거 상담 이력이 열린다.

### 3.2 로그인 전 — 비활성 상태

폼을 숨기지 않고 **어두운 상태로 그대로 보여준다.**

- 화면을 두 벌 만들 필요가 없다
- 로그인하면 뭘 하게 되는지가 그 자체로 서비스 설명이 된다
- 별도의 서비스 소개 영역이 필요 없다

구현은 배경 어둡게(`#2A2A2A`) + 채도 낮춤. **반투명(opacity)은 쓰지 않는다.** 픽셀이 뭉개진다.

로그인 성공 시 조명이 켜지듯 밝아지며 폼이 활성화된다(400ms, ease-out).

### 3.3 사전질문 폼

**필수 9항목을 한 화면에 압축 배치하고, 이벤트가 있을 때만 다음 화면으로 넘어간다.**

밀도 확보를 위해 입력 방식을 압축한다.

| 항목 | 입력 방식 |
|---|---|
| 공연 제목 | 검색 자동완성 (1행) |
| 관람 일시·회차 | 날짜 피커 + 낮공/밤공 토글 (1행) |
| 좌석 위치 | 층·블럭·열·번 인라인 4칸 (1행) |
| 티켓값 | [v1.1] **할인 셀렉트 + 금액** (1행) — 원안의 직접/양도 토글은 changelog v2.1 §1.5에서 폐기됐다. 자격·재관람 할인이나 "목록에 없는 할인"일 때만 증빙 여부 세그먼트가, 증빙 불가+권종 변경 정책일 때만 대체 권종 셀렉트가 조건부로 펼쳐진다. 계산 결과는 판정 없이 사실만 한 줄로 표시(`.fact-line`, changelog v2.1 §1.7) |
| 오늘 캐스팅 | [v1.1] **2택 세그먼트**(출연함/출연 안 함) — 원안의 "3택 + 이름 입력"에서 배우 이름 입력 자체가 빠졌다(§3.1, 매칭 경로 없음) |
| 자첫 여부 | 토글 (좌우 반칸) — `NO`(봤음)일 때만 **작품 선호도 4단계**(인생작/좋아함/무난/안 맞음) 조건부 행이 열린다 |
| 오글 의향 | 토글 (좌우 반칸) |
| 선호 좌석 1·2순위 | **6개 아이콘 그리드**, 탭 순서가 순위 (2행) — `애배 동선` 선택 시 방향(왼/중/오) 조건부 세그먼트 |
| 처분 옵션 | 체크박스 가로 배치, 복수 선택 (1~2행) |
| 기타란 | [v1.1] **기본이 펼친 텍스트영역**(접힌 상태 아님) — changelog v2.1 §2.3: 접어두면 "앞에서 다 채웠으니 더 없다"는 심리로 건너뛰는 사용자가 많았고, 실제로 케이스 스터디 6건 중 2건이 이 칸 때문에 결론이 뒤집혔다 |

**선호 좌석 아이콘 그리드**가 밀도 확보의 핵심이다. 6개를 세로로 나열하면 6행을 먹지만, 2×3 아이콘 그리드면 2행에 들어간다. 각 아이콘은 좌석 위치를 도식화한 5×5 도트 패턴으로 만든다(§2.3).

**직전 상담 응답을 기본값으로 채워둔다**(PRD §3.5). 선호 좌석·오글 의향은 자주 바뀌지 않으므로 대부분 그대로 넘어간다.

#### 이벤트 화면 (조건부)

이벤트가 있으면 두 번째 화면으로 넘어가 PRD §3.3의 5단계를 진행한다. 종류 복수 선택 → 세부 → 끌림도 → 희소성. 없으면 바로 메인으로.

### 3.4 오른쪽 — 제목·설명·좌석

**상단:** 서비스 제목을 크게. 도트체.
**중단:** 한 줄 설명. 일반 폰트.
**하단:** 좌석 한 열 + 스포트라이트.

#### 스포트라이트 = 입력 완성도

**개수가 아니라 밝기와 범위로 표현한다.**

```
완성도 0%    좌석 열이 거의 어둠, 스포트라이트 없음
완성도 50%   중앙 좌석 하나에 약한 조명
완성도 100%  조명이 넓어지고 밝아지며 좌석 열 전체가 보임
```

좌석 개수와 입력 항목 수를 맞출 필요가 없어, 이벤트 조건부로 항목 수가 9~14개로 변해도 대응된다. 비율(`filled / total`)만 계산하면 된다.

**완성도 100%가 되면 조명이 완전히 들어오면서 「상담 시작」 버튼이 활성화**되고, 누르면 화면 전체가 밝아지며 메인으로 전환된다.

---

## 4. 상담실 (메인 작업 페이지)

「상담 시작」을 누르면 들어가는 화면이며, 이름은 **상담실**로 확정한다(`consult.html`). 랜딩의 버튼 문구와 직결된다. **[v1.1] "안내데스크·부스" 은유는 "노을 진 골목에 늘어선 가게 건물"로 바뀌었다** — 아래 §4.1의 칸 구조(2×2 + 컨디션 + 팀장 바)는 원안 그대로지만, 각 칸을 채우는 것이 "부스에 앉은 캐릭터"가 아니라 "보도블록 위에 솟아오르는 건물"이다(§2.4a, §4.5).

### 4.1 레이아웃 — 2×2 그리드 + 하단 팀장 바

```
┌───────────────┬───────────────┐
│               │               │
│   🎭 덕심      │   👁 시야      │
│               │               │
├───────────────┼───────────────┤
│               │               │
│   💰 비용      │   🎁 이벤트    │
│               │               │
├───────────────┴───────────────┤
│  💪 컨디션 (작은 부스, 우측)     │
├───────────────────────────────┤
│                               │
│   [팀장]      가라 / 가지마라    │
│                               │
└───────────────────────────────┘
```

칸 배치(2×2 + 컨디션 가로 칸 + 팀장 바)는 구현과 일치한다. 이모지·"부스" 표기는 원안의 개념도이며, 실제 각 칸은 §4.5의 아이소메트릭 보도블록 타일이다.

**컨디션 배치 주의.** 컨디션은 기타란이 있을 때만 호출되고 없으면 회색 상태다. 2×2 그리드에 넣으면 5칸이 되어 비대칭이 되므로, **그리드와 팀장 바 사이에 가로로 낮은 칸**을 두는 편이 자연스럽다. 활성화 빈도가 가장 낮은 에이전트이므로 시각적 비중도 작아야 한다.

### 4.2 팀장 바

팀장의 출력은 푯말 16자가 아니라 **문단 단위 결론문**이라 정사각형 칸에 들어가지 않는다. 하단 가로 바가 그 공간을 제공하고, "위에서 올라온 의견을 아래에서 취합한다"는 구조도 시각적으로 읽힌다.

**결론은 단어만 크게 항상 표시하고, 이유는 누르면 말풍선으로 펼쳐진다.**

```
결론 확정 전   pose="work" → (5초 후) pose="think"
결론 확정 후   pose="done" — 「가라」 또는 「가지마라」를 크게
              누르면 결론문 + alternative(나눔/취소 제안) 말풍선
```

**[v1.1] 「가라」/「가지마라」는 도트체가 아니라 일반 서체(Pretendard, weight 800)로 크게 표시한다** — §2.4a로 도트체 표시 캐릭터 자체가 사라졌기 때문(원칙 3 참고).

**[v1.1] `pose` 값은 현재 상태 추적용 데이터일 뿐, 포즈별로 다른 그림은 아직 없다.** 팀장 건물 일러스트(`assets/agents/lead.png`)가 아직 제작되지 않아 자리표시자(점선 박스)로 뜨고, `work`/`think` 사이에 시각적 차이가 없다. §11 미결정 항목에 등록.

결론문을 항상 펼쳐두면 화면의 무게추가 결론 쪽으로 넘어가 원칙 4에 어긋난다.

### 4.3 부스 소품

**[v1.1] "배경에 소품을 따로 배치"하지 않는다.** §2.4a에서 정한 대로 소품이 이미 각 에이전트의 건물 일러스트 안(간판·진열대·인물이 든 물건)에 그려져 있어, 처음 온 사용자가 건물 그림만 보고 담당 축을 파악한다. 별도의 배경 오브젝트 레이어가 없다.

로비 정체성은 랜딩의 극장 좌석·스포트라이트가 이미 담당한다.

### 4.4 신호등 [v1.1 신설]

각 건물 칸의 보도블록 꼭짓점에 3색 신호등이 하나씩 서 있다. 축 점수의 부호를 원칙 2(§1)의 두 신호 중 하나로 전달한다.

```
axis_score > 0  → 초록불 (--pos)
axis_score = 0  → 주황불 (--accent, "중립"과 결이 다르므로 팔레트상 강조색을 재사용)
axis_score < 0  → 빨간불 (--neg)
판정 전/is_muted → 꺼짐
```

말풍선 테두리·꼬리와 같은 색 언어를 쓴다 — 신호등을 먼저 보고 내려오면 말풍선 색이 왜 그런지 바로 읽힌다. 팀장 바의 신호등은 최종 판정(`GO`/`NO_GO`)의 초록·빨강만 쓴다.

### 4.5 아이소메트릭 보도블록과 건물 배치 [v1.1 신설]

`ref/angle.jpg`(하이앵글 대각선 뷰)를 참고해, 각 칸 바닥에 마름모 모양 보도블록 타일을 깐다. 윗면 1색 + 옆면 2색(왼쪽이 밝고 오른쪽이 그늘)으로 두께감을 낸다.

**건물은 마름모가 가장 넓어지는 지점(타일의 기하학적 중심)에 발을 붙인다.** 마름모의 꼭짓점(폭이 0인 점)에 붙이면 건물이 타일과 한 점에서만 만나 허공에 떠 있는 것처럼 보이므로 피한다.

**건물은 발밑을 축으로 시계 방향으로 살짝 돌아가 있다** (`--building-lean`, 로컬 변수로 튜닝). 건물 그림이 전부 정면(수직)으로 그려져 있어 대각선 시점의 보도블록과 각도가 안 맞고 상대적으로 앞으로 기울어 보이기 때문에, 그림 자체는 손대지 않고 CSS 회전으로 시각적 각도만 맞춘다.

미완료 상태에서는 건물이 타일 속에 눌려 있다가, 완료되는 순간 같은 위치·같은 각도를 유지한 채 솟아오른다(§2.4a).

---

## 5. 푯말 시스템

### 5.1 상태 [v1.1]

> "캐릭터가 앉다/일어서다"는 원안이며, 실제로는 건물이 있고 밝기와 높이로 상태를 말한다(§2.4a). 말풍선(원안의 "푯말")은 건물이 완료돼야 뜬다.

| 상태 | 표현 |
|---|---|
| 작업 중 | 건물이 타일 속에 눌린 채 어둡다(`brightness .82`). 말풍선 없음 |
| 대기 중 | 작업 중과 동일한 표현 + 상태 텍스트만 다르다(예: 비용의 "시야를 기다리는 중"). 전용 시각 표현은 아직 없음(§11) |
| 완료 | 건물이 밝기를 회복하며(`brightness .82→1`) 솟아오른다(`scale .4→1`). 각도(`--building-lean`)는 상태와 무관하게 항상 동일. 말풍선이 뜬다 |
| `is_muted` | 계속 눌린 채 더 어둡다(`brightness .62`). **말풍선을 띄우지 않는다** |

**회색(=`is_muted`) 건물이 흔들리지 않는 것이 아니라, 말풍선 자체를 안 띄운다.** 빈 말풍선이나 정지한 말풍선보다 "할 말이 없어서 조용하다"가 훨씬 자연스럽다. `detail`도 없으므로 클릭 대상이 아니다.

### 5.2 강도 스펙 [v1.1]

> 원안의 흔들림(진폭·주기·위상 애니메이션)은 구현되지 않았다. 대신 **정지된 두 신호**로 같은 정보를 전달한다(원칙 2) — 접근성 고려사항(`prefers-reduced-motion`)을 위한 대체 표현이 아니라 기본 표현 자체다.

```
말풍선 테두리 굵기 = strength(|axis_score|)
  |axis_score| 0 → 2px (기본)
  |axis_score| 1 → 3px
  |axis_score| 2 → 4px

부호 색(테두리·꼬리·신호등) = sign(axis_score)
  + → --pos(초록)   0 → --accent(주황)   - → --neg(빨강)
```

| `|axis_score|` | 테두리 | 체감 |
|---|---|---|
| 2 | 4px | 강하게 어필 |
| 1 | 3px | 보통 |
| 0 | 2px (기본) | 있지만 강하게 주장하지 않음 |
| `is_muted` | — | 말풍선 없음 |

**점수 0도 테두리 2px로 자기 존재를 보여준다** — 원안의 "최소 진폭 30% 보장"과 같은 목적이다. 완전히 안 보이면 그 의견 자체가 없던 게 된다.

애니메이션은 등장 시 `speechPop`(260ms, 팝업) 하나뿐이라 `prefers-reduced-motion`은 이 등장 연출만 끄면 된다. 강도 표현(테두리 굵기)은 애초에 정지 상태라 별도 대체가 필요 없다.

### 5.3 말풍선

말풍선을 클릭·호버하면 `detail`(100~180자)이 말풍선으로 펼쳐진다. **[v1.1] "픽셀 말풍선"이 아니라 CSS로 그린 일반 말풍선**(둥근 사각형 + border 삼각형 꼬리)이다 — 9-slice 이미지 에셋을 쓰지 않는다(§9.3).

**모달이 아니라 말풍선을 쓰는 이유:**
- 누가 말하는지가 가장 명확하다
- 게임 대사창 문법이라 화면 톤과 맞는다
- **열어둔 채 다른 말풍선을 누르면 바로 옮겨간다.** 5개 의견을 견주는 것이 이 서비스의 핵심이므로 닫고 여는 과정이 없어야 한다

**말풍선이 옆 칸을 덮는 것은 문제가 아니다.** 게임에서 대사창이 화면을 덮는 것과 같은 관습이다.

**[v1.1] 방향은 고정 표(위/아래)가 아니라 실측 좌표로 매 순간 계산한다.** 앵커가 `.room` 높이의 45% 위쪽이면 아래로, 아니면 위로 펼치고, 가로는 화면 중심 기준 좌/우로 치우쳐 펼친다. 아래 표는 이 로직이 실제로 만들어내는 결과와 같다(레이아웃이 바뀌어도 코드가 스스로 재계산하므로 표를 손으로 맞출 필요가 없다):

```
상단 2칸  → 아래로 펼침
하단 2칸  → 위로 펼침
컨디션    → 위로 펼침
팀장      → 위로 펼침 (화면 중앙까지 확장 가능)
좌측 칸   → 오른쪽으로 치우쳐 펼침
우측 칸   → 왼쪽으로 치우쳐 펼침
```

말풍선 꼬리는 항상 해당 건물을 가리킨다 — 그림의 실제 렌더링 박스를 매 프레임 재서(`layoutTails`) 꼬리 위치를 맞춘다(오브젝트 여백이 칸마다 달라 고정값을 못 씀).

**말풍선 본문은 일반 폰트를 쓴다**(원칙 3). 100~180자를 도트체로 읽는 것은 부담이다.

---

## 6. 로딩 연출

### 6.1 순차 등장 — 실제 응답 순서 + 최소 간격

```
API 응답 도착 → 큐에 적재 → 앞 에이전트 등장 후 최소 1000ms 경과 시 다음 등장
```

**실제 순서를 쓰되 최소 간격을 보장한다.** 정직하면서도 3~4명이 동시에 일어서서 뭉쳐 보이는 문제를 피한다.

**비용 에이전트가 항상 마지막인 것은 의도된 결과다.** 시야 출력을 받아야 판단할 수 있는 파이프라인 구조(PRD §6.2)가 화면에 그대로 드러난다. 다른 에이전트가 일하는 동안 비용은 **"시야를 기다리는 중"** 상태 텍스트로 있다가 시야가 끝나면 "작업 중"으로 바뀐다. **[v1.1]** 원안은 이때 전용 "대기 포즈" 그림을 상정했지만, 실제로는 건물 그림이 작업 중과 동일하고(§5.1) 상태 텍스트만 바뀐다 — 사용자가 이유를 알 필요는 없지만, 문구를 보고 있으면 "저기는 뭔가를 기다렸구나"가 읽힌다.

컨디션 에이전트는 기타란이 없으면 호출되지 않으므로 **처음부터 완료 이전 상태**로 시작한다.

### 6.2 팀장 지연 대응 [v1.1]

4명이 다 완료됐는데 결론이 아직 안 나온 경우.

| 경과 | 원안(포즈 스프라이트) | 실제 구현 |
|---|---|---|
| 0~5초 | 서류 검토 루프 | `pose="work"` — 시각 차이 없음(§4.2, 팀장 건물 미제작) |
| 5~10초 | **"고민 중" 포즈** | `pose="think"`로 전환되지만 대응 CSS가 없어 그림은 그대로 |
| 10~20초 | 고민 포즈 유지, 미세 동작 추가 | 별도 단계 없음 — think 상태 유지 |
| 20초~ | **타임아웃 안내** | 그대로 구현됨 (아래) |

**포즈별 그림 차이는 §11의 미결정 항목이다.** `data-pose` 속성은 이미 타임라인대로 바뀌고 있으므로, 팀장 건물 에셋이 생기고 포즈별 프레임(또는 필터 변화)을 붙이면 이 표는 원안대로 복원된다. 20초 타임아웃 안내만은 포즈 그림과 무관하게 이미 완전히 동작한다.

**20초 초과 시 타임아웃 처리:**
```
"시간이 조금 걸리고 있어요"
[ 계속 기다리기 ]  [ 다시 시도 ]
```

안내가 없으면 사용자가 새로고침해 상담 결과가 날아간다. 이때만 텍스트가 등장하며, 평소에는 진행 상태 문구를 쓰지 않는다.

### 6.3 결론 등장

팀장 작업이 끝나면 결론 단어가 팀장 바에 나타난다. 별도의 극적 연출(화면 중앙 스포트라이트 등)은 넣지 않는다. 매번 보면 성가시고, 원칙 4에 따라 결론이 푯말보다 무거워지면 안 된다.

---

## 7. 타이포그래피

### 7.1 폰트

| 용도 | 폰트 | 라이선스 |
|---|---|---|
| 도트체 (**랜딩 한정** [v1.1]) | **Galmuri11 / Galmuri14** | SIL OFL 1.1 |
| 본문 (**상담실은 전 텍스트**, 랜딩은 말풍선 등 일부) | **Pretendard** | SIL OFL 1.1 |
| 대체 | Noto Sans KR | SIL OFL 1.1 |

<cite index="8-1">Galmuri는 SIL 오픈 폰트 라이선스 1.1에 따라 사용할 수 있으며, 폰트가 자체적으로 판매되지 않는 한 자유롭게 사용·연구·수정·재배포할 수 있다.</cite> <cite index="9-1">상업적 무료 글꼴이라 해도 폰트 파일 자체를 파는 행위는 금지된다.</cite>

**Galmuri 크기별 선택 (랜딩):** 버튼·제목은 Galmuri14, 그 외 작은 라벨은 Galmuri11. 도트 폰트는 지정된 픽셀 크기의 정수배로만 렌더링해야 뭉개지지 않는다. **상담실은 Galmuri를 로드하지 않는다** — 말풍선·결론·버튼 전부 Pretendard 한 서체(`--ui`)로 통일했다(원칙 3 [v1.1]).

### 7.2 크기 체계

```
[랜딩]
서비스 제목      Galmuri14, clamp(28px,4vw,40px)
「상담 시작」     Galmuri14, 15px
버튼            Galmuri11, 11px

[상담실 — 전부 Pretendard, v1.1]
결론 (가라)      weight 800, clamp(30px,4.6vw,40px)
말풍선(=푯말)    weight 600, 17px (길면 13px로 축소, §7.3)
축 이름 라벨     weight 600, 13px
버튼            weight 600, 12px

[공통]
말풍선 상세(detail) 본문   Pretendard 15px / 행간 1.7
입력 폼 레이블            Pretendard 13px
입력값                   Pretendard 15px
공연 정보·금액            Pretendard 15px (tabular-nums)
보조 설명                 Pretendard 12px
```

금액과 좌석 번호에는 `font-variant-numeric: tabular-nums`를 적용해 자릿수가 흔들리지 않게 한다.

### 7.3 말풍선(=푯말) 텍스트 처리 [v1.1]

PRD가 `placard` 최대 16자를 보장하지만 방어 코드가 필요하다. 초과 시 말줄임 대신 **폰트 크기를 한 단계 내린다**(`.speech.long`, 17px에서 13px로). **임계값은 16자가 아니라 12자다** — PRD의 데이터 계약(16자)보다 화면 쪽을 보수적으로 잡아, 실측상 자주 나오는 12~15자 구간도 축소 처리로 여유 있게 받는다. Pretendard는 도트 폰트와 달리 임의 크기로 줄일 수 있으므로 정수배 제약이 없다. **말줄임(ellipsis) 폴백은 아직 코드에 없다** — 두 단계 방어가 아니라 폰트 축소 한 단계뿐이며, PRD의 16자 상한을 신뢰해 그 이상은 방어하지 않는다.

---

## 8. 반응형

### 8.1 원칙 [v1.1]

**랜딩은 정수배 스케일을 쓴다.** 픽셀 요소(좌석·선호 좌석 아이콘)는 브레이크포인트마다 배율을 바꾸고, 중간 크기는 여백으로 흡수한다.

| 폭 | 배율 | 레이아웃 |
|---|---|---|
| ≥1440px | ×3 | 기본 |
| 1024~1439px | ×2 | 기본 |
| 768~1023px | ×2 | 랜딩: 상하 분할 |
| <768px | ×2 | §8.2 |

**상담실은 이 표를 따르지 않는다.** 건물 일러스트가 일반 이미지(§2.3)라 정수배 제약이 없고, 칸 크기는 `--tile: clamp(84px, min(23vh, 19vw), 290px)` 하나로 뷰포트 폭·높이 양쪽에 유동적으로 반응한다. 브레이크포인트는 글자 크기 조정에만 쓴다(1023px, 767px — 아래 §8.2).

### 8.2 모바일 [v1.1]

PWA 우선 전략(PRD §12.3)이므로 모바일이 실사용 환경일 가능성이 높다.

**랜딩:** 좌우 분할을 상하로 전환. 제목·좌석 조명이 위, 폼이 아래. 스포트라이트는 화면 상단에 가로로 눕힌다.

**상담실:** 2×2 그리드 + 컨디션·팀장 칸 구조는 그대로 유지한다(원안처럼 세로 스택으로 재배치하지 않는다) — `--tile` clamp의 가로(`vw`) 상한만 완화해 좁은 화면에서도 칸이 찌그러지지 않게 한다. 말풍선만 화면 하단 고정 시트로 전환한다(게임 대사창과 동일, 원안 유지).

**터치 대응:** 호버가 없으므로 말풍선 탭이 유일한 진입점이다. 탭 영역을 말풍선보다 넉넉하게(최소 44×44px) 잡는다.

---

## 9. 에셋 목록

> [v1.1] 이 절은 원안(프레임 단위 픽셀 캐릭터)을 기준으로 쓰였다. 상담실은 §2.4a의 방식(에이전트당 정적 일러스트 1장)으로 완전히 바뀌었으므로 9.1·9.3·9.4를 다시 쓴다. 랜딩은 원안 그대로라 손대지 않는다.

### 9.1 상담실 — 건물 일러스트 [v1.1]

| 에셋 | 상태 | 비고 |
|---|---|---|
| 덕심 건물 (`deoksim.png`) | **제작 완료** | 560×420, MD SHOP·캐스팅보드 |
| 시야 건물 (`siya.png`) | **제작 완료** | 560×420, 시야 분석 센터 |
| 비용 건물 (`cost.png`) | **제작 완료** | 560×420, 티켓 가격 분석소 |
| 이벤트 건물 (`event.png`) | **제작 완료** | 560×420, 이벤트 연구소 |
| 컨디션 건물 (`condition.png`) | **미제작** | 로드 실패 시 점선 자리표시자(`.art-slot`)로 대체 |
| 팀장 건물 (`lead.png`) | **미제작** | 상동. §11 미결정 항목 |

프레임 애니메이션은 없다. 상태 변화는 전부 CSS `filter`(밝기·채도)와 `transform`(scale/translateY, §2.4a)으로 처리하므로 에이전트당 이미지 1장이면 모든 상태를 표현한다.

### 9.2 소품·오브젝트

**[v1.1] 별도 레이어로 존재하지 않는다.** 캐스팅보드·오페라글라스·계산기·증정품 상자 등은 각 건물 일러스트 안에 이미 합성되어 있다(§2.4a 표). 랜딩에만 남아 있는 독립 UI 오브젝트는 §9.3 참고.

### 9.3 UI

**[v1.1]**

| 에셋 | 구현 방식 |
|---|---|
| 말풍선(=푯말) | CSS만 (둥근 사각형 + `border` 삼각형 꼬리) — 9-slice 이미지 아님 |
| 보도블록 타일 | CSS `clip-path` 마름모 3장(윗면·좌측면·우측면) — 이미지 아님 |
| 신호등 | CSS(원+사각형 조합) — 이미지 아님 |
| 극장 좌석 (랜딩) | CSS 도형 (1좌석 단위) |
| 스포트라이트 (랜딩) | CSS `radial-gradient`, 밝기·범위 연속 보간 |
| 선호 좌석 아이콘 (랜딩) | 5×5 도트 패턴 6종 |
| 버튼 | CSS (활성/비활성/호버) |

**상담실 UI는 이미지 에셋이 0장이다** — 건물(§9.1)을 뺀 나머지 전부가 CSS로 그려진다.

### 9.4 제작 도구 [v1.1]

| 작업 | 도구 |
|---|---|
| 건물 일러스트 (상담실) | 이미지 생성, 그대로 사용 |
| 랜딩 아이콘·좌석 등 픽셀 요소 | 직접 구현(CSS 도트 패턴) |
| 레이아웃·컴포넌트 | 직접 구현 |

**Aseprite 등 프레임 단위 도트 툴은 현재 쓰지 않는다.** 상담실에 프레임 애니메이션이 없고(§9.1), 랜딩의 픽셀 요소도 이미지가 아니라 CSS 그리드로 직접 찍기 때문이다. §10.2의 "AI 생성물을 도트 툴에서 손으로 재작업한다"는 저작권 대응 방침은 건물 일러스트에도 그대로 적용해야 하는지 미확인 상태다 — §11 참고.

---

## 10. 라이선스 — 수익화 대비

**전제: 향후 유료화·광고·투자 유치 등 어떤 형태로든 수익이 발생해도 문제가 없어야 한다.**

### 10.1 폰트

| 폰트 | 라이선스 | 상업 이용 | 웹 임베딩 |
|---|---|---|---|
| Galmuri (전 시리즈) | SIL OFL 1.1 | ✅ | ✅ |
| Pretendard | SIL OFL 1.1 | ✅ | ✅ |
| Noto Sans KR | SIL OFL 1.1 | ✅ | ✅ |

**OFL의 유일한 제약:** 폰트 파일 자체를 판매할 수 없다. 서비스에 임베딩하는 것은 자유다.

**금지:** 라이선스가 "개인 비영리만 무료"이거나 "300인 이하 기업만" 같은 조건부인 폰트. 무료로 배포되는 한글 폰트 중 상당수가 여기 해당하므로 반드시 개별 확인한다.

**self-host 권장.** CDN이 중단되면 폰트가 깨진다. OFL은 재배포를 허용하므로 직접 호스팅해도 된다. 라이선스 전문을 함께 배포한다.

### 10.2 AI 생성 이미지 에셋 (랜딩 픽셀 요소 + 상담실 건물 일러스트) [v1.1 제목 갱신]

AI 이미지 생성으로 제작한 캐릭터는 **사용에는 문제가 없으나, 두 가지를 인지해야 한다.**

1. **생성 서비스 약관을 확인할 것.** 상업적 이용 허용 여부와 출력물 소유권 조항은 서비스마다 다르고 변경될 수 있다.
2. **AI 생성물은 저작권 보호를 받지 못할 수 있다.** 인간 창작성이 인정되지 않으면 저작권이 발생하지 않으며, 그 경우 **제3자가 그대로 가져다 써도 막을 수단이 없다.** 서비스가 성장했을 때 실질적 리스크가 된다.

**권장 대응:** 초안은 AI로 뽑되, **손으로 수정·재작업한 최종본을 사용한다.** 인간의 창작적 수정이 들어가면 그 부분에 대해 저작권 주장이 가능해진다.

**[v1.1] 상담실의 건물 일러스트(§2.4a, §9.1)도 AI 생성물이며 같은 리스크를 그대로 안는다.** 애니메이션 프레임을 직접 찍는 방식(원안 §9.4)이 사라지면서 "제작 과정에 인간의 창작적 수정이 들어간다"는 보장 수단도 함께 없어졌다 — 현재 생성 결과를 손 보정 없이 그대로 쓰고 있는지 확인되지 않았다. §11에 미결정 항목으로 등록.

### 10.3 사운드

현재 사양에 사운드는 없다. 추가한다면 CC0 또는 상업 이용 명시 라이선스만 사용한다.

### 10.4 PRD 측 라이선스 이슈

디자인 외 영역에서 수익화 시 문제가 될 수 있는 항목이다. **`prd.md` 부록 B에 상세를 둔다.**

- 크롤링 데이터의 데이터베이스제작자 권리
- 예매처 이용약관의 자동 수집 금지 조항
- 좌석배치도 이미지 복제 금지 (사실 정보만 추출)
- 공연 포스터·스틸 이미지 사용 금지
- 배우 초상·퍼블리시티권
- 서비스명 상표 확인
- LLM API 상업 이용 조건
- 수익화 시 이용약관·개인정보처리방침 정비

---

## 11. 미결정 항목 [v1.1 갱신]

| 항목 | 내용 |
|---|---|
| 팀장·컨디션 건물 에셋 | `lead.png`, `condition.png` 미제작 — 로드 실패 시 점선 자리표시자로 대체됨(§9.1) |
| 팀장 포즈별 표현 | `data-pose`(work/think/done) 값은 이미 갱신되지만 대응 CSS·이미지가 없어 시각 차이가 없음(§6.2). 건물 에셋이 생기면 포즈별 필터/프레임을 붙일지 결정 |
| 컨디션 "대기 중" 시각 표현 | 현재 작업 중과 동일한 그림 + 상태 텍스트만 다름(§6.1). 전용 표현이 필요한지 |
| 건물 일러스트의 저작권 대응 | AI 생성물을 손으로 재작업했는지 미확인(§10.2) |
| `--building-lean` 각도 | 6deg → 14deg로 상향 조정한 상태(2026-08-25 튜닝 중). 실물 확인 후 최종값 확정 |
| 말풍선 최대 폭 | 360px로 구현. 100~180자가 몇 줄로 떨어지는지 확인 후 확정 |
| 이용 내역 모달 내용 | 과거 상담 목록 표시 형식 |
| 모바일 말풍선 | 하단 고정 시트 전환 시 말풍선 비교 경험을 어떻게 유지할지 |

### 확정된 항목 [v1.1 갱신]

| 항목 | 결정 |
|---|---|
| 화면 이름 | **상담실** (`consult.html`) — §4 |
| 상담실 아트 방향 | "부스에 앉은 도트 캐릭터" 원안을 폐기하고 **"보도블록 골목에 늘어선 건물"**로 확정(§2.4a). 도트체·프레임 애니메이션·흔들림 전부 걷어냄 |
| 컨디션 칸 형태 | 그리드와 팀장 바 사이 **가로로 낮은 칸** |
| 강도 표현 | 흔들림 대신 **말풍선 테두리 굵기**(2/3/4px) + **신호등 색**(빨강/주황/초록) (§5.2, §4.4) |
| 건물 착지 위치 | 보도블록 마름모가 가장 넓어지는 지점(기하학적 중심)에 발을 맞춤. 꼭짓점에 맞추면 한 점에서만 닿아 떠 보임(§4.5) |
| 건물 각도 | 발밑을 축으로 시계 방향 회전(`--building-lean`)으로 정면 그림과 대각선 지면 각도를 맞춤(§4.5) |
| 선호 좌석 아이콘 도안 | 5×5 도트 패턴 6종, 노란색 하나 + 불투명도 3단계 (`index.html`) |
| 전환 애니메이션 | 조명 오버레이(`.transition-overlay`) 후 페이지 이동. 폼 데이터는 `sessionStorage` |

---

## 부록 A. 구현 체크리스트

### 픽셀 아트 (랜딩 한정 [v1.1])
- [ ] `image-rendering: pixelated` 적용 (좌석·선호 좌석 아이콘 등 랜딩의 픽셀 요소)
- [ ] 스케일이 정수배인가 (×2 / ×3 / ×4)
- [ ] 반투명(opacity)으로 비활성 처리하고 있지 않은가 → 밝기·채도로 처리
- [ ] 도트 폰트가 지정 픽셀 크기의 정수배로 렌더링되는가
- [ ] **상담실의 건물 일러스트는 이 체크리스트 대상이 아니다** — `image-rendering:auto`가 의도된 값이다(§2.4a)

### 말풍선(=푯말) [v1.1]
- [ ] `is_muted`일 때 말풍선을 띄우지 않는가 (빈 말풍선 금지)
- [ ] 테두리 굵기가 `|axis_score|`에 비례하는가 (2/3/4px, §5.2)
- [ ] 점수 0도 기본 굵기(2px)로 존재를 드러내는가
- [ ] 부호 색(신호등·테두리·꼬리)이 pos/neu/neg에 맞게 일치하는가
- [ ] `prefers-reduced-motion` 시 등장 팝업 애니메이션이 꺼지는가
- [ ] 말풍선 텍스트 12자 초과 시 폰트 축소 로직이 있는가 (§7.3)
- [ ] 열어둔 채 다른 말풍선을 누르면 옮겨가는가 (닫고 여는 과정 없음)
- [ ] 위치에 따라 펼침 방향이 자동 결정되는가
- [ ] 본문이 일반 폰트인가 (상담실은 전 텍스트가 Pretendard — 도트체 자체가 없음)

### 로딩
- [ ] 실제 응답 순서를 쓰되 최소 1초 간격이 보장되는가
- [ ] 비용 에이전트가 시야를 기다리는 동안 "시야를 기다리는 중" 텍스트로 구분되는가
- [ ] 컨디션 미호출 시 처음부터 미완료(눌린) 상태로 시작하는가
- [ ] 20초 초과 시 타임아웃 안내가 뜨는가

### 랜딩
- [ ] 로그인 전 폼이 숨겨지지 않고 어둡게 표시되는가
- [ ] 스포트라이트가 개수가 아니라 밝기·범위로 진행을 표현하는가
- [ ] 항목 수가 9~14개로 변해도 완성도 계산이 동작하는가
- [ ] 직전 상담 응답이 기본값으로 채워지는가

### 라이선스
- [ ] 사용 중인 모든 폰트가 상업 이용 가능한가
- [ ] 폰트 라이선스 전문이 함께 배포되는가
- [ ] 픽셀 에셋(랜딩)·건물 일러스트(상담실) 모두 인간의 창작적 수정이 들어갔는가 [v1.1] — 건물 일러스트는 §11 미확인
- [ ] 조건부 무료 폰트가 섞여 있지 않은가

### PRD 정합성
- [ ] 말풍선 문구가 자기 축의 언어인가 (판단 로직은 PRD 소관)
- [ ] 결론이 말풍선보다 시각적으로 무거워지지 않았는가
- [ ] 축 점수 색이 말풍선 바탕 전체를 칠하고 있지 않은가 (테두리·꼬리·신호등에만, §2.2)
