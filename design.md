# 「이 회차 갈까말까」 design.md

> **문서 범위**
> 화면 구조, 씬 구성, 아트 디렉션, 애니메이션, 타이포그래피, 에셋 목록을 규정한다.
> **판단 로직·에이전트 명세·데이터 구조는 `prd.md`에 있으며 여기서 반복하지 않는다.**
> PRD가 정한 데이터 계약(푯말 16자, detail 100~180자, `is_muted`, 축 점수 -2~+2)을 제약 조건으로 받는다.

| 항목 | 내용 |
|---|---|
| 버전 | v1.0 |
| 작성일 | 2026-08-16 |
| 아트 스타일 | **픽셀 아트 (도트)** |
| 화면 수 | **2** (랜딩 / 메인 작업) |
| 부속 문서 | `prd.md`, `test_cases.md` |

---

## 1. 디자인 원칙

### 원칙 1 — 조명이 전체를 관통하는 언어다

밝기 변화가 이 서비스의 유일한 상태 표현 수단이다. 별도의 프로그레스 바나 로딩 스피너를 쓰지 않는다.

```
로그인 전   폼 영역 어두움
로그인      조명이 켜지며 폼 활성화
입력 진행   오른쪽 좌석 스포트라이트가 점점 밝고 넓어짐
입력 완료   조명이 완전히 들어오며 메인으로 전환
```

공연장의 조명 은유가 뮤지컬 서비스라는 정체성과 직결되며, 어떤 진행률 표시보다 맥락에 맞는다.

### 원칙 2 — 푯말 흔들림 자체가 정보다

사용자가 텍스트를 읽기 **전에** 화면의 움직임만으로 "오늘은 두 명이 강하게 말하고 있구나"를 파악할 수 있어야 한다.

진폭과 속도는 `|axis_score|`에 비례하고, 위상은 에이전트마다 다르게 하여 리듬을 만든다. `is_muted`는 정지.

### 원칙 3 — 도트체는 캐릭터가 말할 때만

| 폰트 | 쓰는 곳 |
|---|---|
| **도트체 (갈무리)** | 푯말, 결론(가라/가지마라), 버튼, 씬 내 텍스트 |
| **일반 폰트 (Pretendard)** | 말풍선 본문, 입력 폼, 공연 정보, 금액·좌석 번호 |

기준은 **"캐릭터가 말하는 것"** vs **"시스템이 보여주는 정보"**다. 공연 제목·배우 이름·좌석 번호·금액은 정확히 읽혀야 하므로 도트체를 쓰지 않는다.

### 원칙 4 — 5개 푯말이 화면의 주인공이다

PRD §1.3에 따라 결론보다 푯말이 핵심 가치다. 결론은 단어 하나로 크게 보여주되, 화면 면적과 시선은 푯말이 가져간다.

### 원칙 5 — 무료·상업 이용 가능한 에셋만 사용한다

폰트·아이콘·사운드 등 모든 외부 에셋은 상업적 이용이 명시적으로 허용된 것만 쓴다(§10).

---

## 2. 아트 디렉션

### 2.1 톤

**중간 회색조 기반.** 너무 어둡지도 밝지도 않으며, 조명 영역만 확실히 밝다.

| 이유 | |
|---|---|
| 조명 대비 확보 | 어두운 배경이면 조명 연출이 살지만 긴 본문 가독성이 떨어짐 |
| 캐릭터 재활용 | 기존 스프라이트가 어두운 회색 배경 기준으로 제작됨 |
| 본문 가독성 | 말풍선 안 100~180자를 읽어야 함 |

랜딩과 메인 모두 같은 톤을 쓴다. 캐릭터를 두 벌 만들지 않는다.

### 2.2 컬러 팔레트

픽셀 아트는 색 수를 제한할수록 완성도가 올라간다. **16색 이내**로 운용한다.

```
배경        #3A3A3A  (중간 회색, 기본)
배경 어두움  #2A2A2A  (비활성 영역)
배경 밝음    #4A4A4A  (부스 내부)

조명        #F5E6C8  (스포트라이트, 따뜻한 백색)
조명 약     #8A7F6A  (조명 감쇠 구간)

텍스트 주   #F0F0F0
텍스트 보조  #A0A0A0
텍스트 비활성 #6A6A6A

강조        #E5A93C  (팀장 결론, 강한 신호. 기존 이미지의 노란 포인트)
긍정        #7FB069  (+ 축 신호)
부정        #C4674F  (- 축 신호)
중립        #8A8A8A  (0 축 신호)

푯말 바탕   #E8DFC8  (기존 이미지의 나무 푯말 톤)
푯말 테두리  #6B5842
```

**축 점수 색은 푯말 테두리나 작은 인디케이터에만 쓴다.** 푯말 바탕 전체를 색으로 칠하면 화면이 신호등처럼 보이고, 무엇보다 사용자에게 "이건 좋은 것/나쁜 것"을 강하게 주입하게 되어 PRD 원칙 1(어떤 에이전트도 가라/가지마라를 말하지 않는다)에 어긋난다.

### 2.3 스프라이트 규격

| 항목 | 값 |
|---|---|
| 기본 캐릭터 | **64 × 96 px** (전신) |
| 착석 캐릭터 | 64 × 64 px |
| 푯말 | 80 × 48 px |
| 소품 | 32 × 32 px 기준 |
| 픽셀 스케일 | 정수배만 (×2, ×3, ×4). 소수배 금지 |
| 렌더링 | `image-rendering: pixelated` 필수 |

**정수배 스케일 원칙이 반응형 설계를 지배한다.** 브레이크포인트마다 ×2 / ×3 / ×4로 전환하며, 중간 크기는 여백으로 흡수한다.

### 2.4 캐릭터

기존 제작 에셋을 기준으로 한다.

- **팀장** — 헤드셋 착용. 하단 바 중앙
- **관점 에이전트 4인** — 헤드셋 없음. 상단 2×2 그리드
- 4인은 동일 베이스에 **소품과 색 변주**로 구분한다. 완전히 다른 캐릭터를 5개 그리면 픽셀 아트 물량이 감당되지 않고 일관성도 깨진다

| 에이전트 | 구분 요소 |
|---|---|
| 덕심 | 캐스팅보드, 배우 프로필 시트 |
| 시야 | 좌석배치도, 오페라글라스 |
| 비용 | 계산기, 티켓, 영수증 |
| 이벤트 | 증정품 상자, 폴라로이드 |
| 팀장 | 헤드셋, 안내데스크 |

---

## 3. 랜딩 페이지

### 3.1 레이아웃

```
┌─────────────────────────┬─────────────────────────┐
│ [로그인]  또는  [이용내역] │                         │
│  ← 상단에 작게            │      이 회차             │
├─────────────────────────┤      갈까말까            │
│                         │                         │
│                         │   다섯 명이 각자의 관점에서 │
│      사전질문 폼          │   이 회차를 판단합니다     │
│      (왼쪽 전체)          │                         │
│                         │  ▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁      │
│                         │  ┃┃┃┃┃┃┃┃┃┃┃┃┃┃      │
│                         │      좌석 한 열           │
│                         │    (스포트라이트)         │
│              [상담 시작]  │                         │
└─────────────────────────┴─────────────────────────┘
```

**로그인은 상단에 작게.** 왼쪽 절반을 차지하지 않는다. 로그인 후에는 같은 자리에 **이용 내역 버튼**만 남고, 누르면 모달로 과거 상담 이력이 열린다.

### 3.2 로그인 전 — 비활성 상태

폼을 숨기지 않고 **어두운 상태로 그대로 보여준다.**

- 화면을 두 벌 만들 필요가 없다
- 로그인하면 뭘 하게 되는지가 그 자체로 서비스 설명이 된다
- 별도의 서비스 소개 영역이 필요 없다

구현은 배경 어둡게(`#2A2A2A`) + 채도 낮춤. **반투명(opacity)은 쓰지 않는다.** 픽셀이 뭉개진다.

로그인 성공 시 조명이 켜지듯 밝아지며 폼이 활성화된다(400ms, ease-out).

### 3.3 사전질문 폼

**필수 9항목을 한 화면에 압축 배치하고, 이벤트가 있을 때만 다음 화면으로 넘어간다.**

밀도 확보를 위해 입력 방식을 압축한다.

| 항목 | 입력 방식 |
|---|---|
| 공연 제목 | 검색 자동완성 (1행) |
| 관람 일시·회차 | 날짜 피커 + 낮공/밤공 토글 (1행) |
| 좌석 위치 | 층·블럭·열·번 인라인 4칸 (1행) |
| 티켓값 | 직접/양도 토글 + 금액 (1행, 양도 시 추가 결제란 확장) |
| 애배 출연 | 3택 세그먼트 + 이름 입력 (1행) |
| 자첫 여부 | 토글 (좌우 반칸) |
| 오글 의향 | 토글 (좌우 반칸) |
| 선호 좌석 1·2순위 | **6개 아이콘 그리드**, 탭 순서가 순위 (2행) |
| 처분 옵션 | 체크박스 가로 배치, 복수 선택 (1~2행) |
| 기타란 | 접힌 텍스트영역 (1행, 펼치면 확장) |

**선호 좌석 아이콘 그리드**가 밀도 확보의 핵심이다. 6개를 세로로 나열하면 6행을 먹지만, 2×3 아이콘 그리드면 2행에 들어간다. 각 아이콘은 좌석 위치를 도식화한 32×32 픽셀 아트로 만든다.

**직전 상담 응답을 기본값으로 채워둔다**(PRD §3.5). 선호 좌석·오글 의향은 자주 바뀌지 않으므로 대부분 그대로 넘어간다.

#### 이벤트 화면 (조건부)

이벤트가 있으면 두 번째 화면으로 넘어가 PRD §3.3의 5단계를 진행한다. 종류 복수 선택 → 세부 → 끌림도 → 희소성. 없으면 바로 메인으로.

### 3.4 오른쪽 — 제목·설명·좌석

**상단:** 서비스 제목을 크게. 도트체.
**중단:** 한 줄 설명. 일반 폰트.
**하단:** 좌석 한 열 + 스포트라이트.

#### 스포트라이트 = 입력 완성도

**개수가 아니라 밝기와 범위로 표현한다.**

```
완성도 0%    좌석 열이 거의 어둠, 스포트라이트 없음
완성도 50%   중앙 좌석 하나에 약한 조명
완성도 100%  조명이 넓어지고 밝아지며 좌석 열 전체가 보임
```

좌석 개수와 입력 항목 수를 맞출 필요가 없어, 이벤트 조건부로 항목 수가 9~14개로 변해도 대응된다. 비율(`filled / total`)만 계산하면 된다.

**완성도 100%가 되면 조명이 완전히 들어오면서 「상담 시작」 버튼이 활성화**되고, 누르면 화면 전체가 밝아지며 메인으로 전환된다.

---

## 4. 메인 작업 페이지

### 4.1 레이아웃 — 2×2 그리드 + 하단 팀장 바

```
┌───────────────┬───────────────┐
│               │               │
│   🎭 덕심      │   👁 시야      │
│               │               │
├───────────────┼───────────────┤
│               │               │
│   💰 비용      │   🎁 이벤트    │
│               │               │
├───────────────┴───────────────┤
│  💪 컨디션 (작은 부스, 우측)     │
├───────────────────────────────┤
│                               │
│   [팀장]      가라 / 가지마라    │
│                               │
└───────────────────────────────┘
```

**컨디션 배치 주의.** 컨디션은 기타란이 있을 때만 호출되고 없으면 회색 상태다. 2×2 그리드에 넣으면 5칸이 되어 비대칭이 되므로, **그리드와 팀장 바 사이에 가로로 낮은 부스**를 두는 편이 자연스럽다. 활성화 빈도가 가장 낮은 에이전트이므로 시각적 비중도 작아야 한다.

### 4.2 팀장 바

팀장의 출력은 푯말 16자가 아니라 **문단 단위 결론문**이라 정사각형 칸에 들어가지 않는다. 하단 가로 바가 그 공간을 제공하고, "위에서 올라온 의견을 아래에서 취합한다"는 구조도 시각적으로 읽힌다.

**결론은 단어만 크게 항상 표시하고, 이유는 누르면 말풍선으로 펼쳐진다.**

```
결론 확정 전   팀장이 대기 포즈 → 작업 포즈
결론 확정 후   「가라」 또는 「가지마라」를 도트체로 크게
              누르면 결론문 + alternative(나눔/취소 제안) 말풍선
```

결론문을 항상 펼쳐두면 화면의 무게추가 결론 쪽으로 넘어가 원칙 4에 어긋난다.

### 4.3 부스 소품

배경 로비 오브젝트를 크게 그리지 않는다. 2×2 그리드와 팀장 바가 화면 대부분을 차지해 배경이 실제로 보일 면적이 거의 없다.

대신 **각 부스 안에 그 축의 소품**을 배치한다. 처음 온 사용자가 소품만 보고 담당 축을 파악할 수 있어야 한다(§2.4 표).

로비 정체성은 랜딩의 극장 좌석·스포트라이트가 이미 담당한다.

---

## 5. 푯말 시스템

### 5.1 상태

| 상태 | 표현 |
|---|---|
| 작업 중 | 캐릭터가 앉아서 타이핑 루프. 푯말 없음 |
| 대기 중 | 캐릭터가 앉아서 대기 포즈. 푯말 없음 (비용 에이전트가 시야를 기다릴 때) |
| 완료 | 캐릭터가 일어서서 푯말을 듦. 흔들림 시작 |
| `is_muted` | 캐릭터가 앉아만 있음. **푯말을 들지 않는다** |

**회색 푯말을 흔들지 않는 것이 아니라, 아예 들지 않는다.** 빈 푯말이나 정지한 푯말보다 "할 말이 없어서 자리에 앉아 있다"가 훨씬 자연스럽다. `detail`도 없으므로 클릭 대상이 아니다.

### 5.2 흔들림 스펙

```
진폭 = BASE_AMP × (0.3 + 0.7 × |axis_score| / 2)
주기 = BASE_PERIOD × (1.4 - 0.4 × |axis_score| / 2)
위상 = 에이전트 인덱스마다 다른 오프셋
```

| `|axis_score|` | 진폭 | 체감 |
|---|---|---|
| 2 | 최대 | 강하게 어필 |
| 1 | 중간 | 보통 |
| 0 | 최소 (30%) | 미세하게 움직임 |
| `is_muted` | 0 | 푯말 없음 |

**최소 진폭 30%를 보장하는 이유:** 점수 0인 에이전트가 완전히 정지하면 사용자가 그 의견을 아예 안 본다. 미세하게라도 움직이면 "여기도 뭔가 있다"가 전달된다.

위상을 어긋나게 해 5개가 동시에 같은 위치에 있지 않도록 한다. 우연히 맞았다 어긋나는 리듬이 생겨 어지러움이 줄고 생동감이 살아난다.

권장 초기값: `BASE_AMP` 8px(×2 스케일 기준), `BASE_PERIOD` 500ms.

**`prefers-reduced-motion` 대응 필수.** 설정 시 흔들림을 정지하고 진폭 대신 푯말 테두리 굵기나 작은 인디케이터로 강도를 표현한다.

### 5.3 말풍선

푯말을 클릭·호버하면 `detail`(100~180자)이 픽셀 말풍선으로 펼쳐진다.

**모달이 아니라 말풍선을 쓰는 이유:**
- 누가 말하는지가 가장 명확하다
- 게임 대사창 문법이라 픽셀 아트와 맞는다
- **열어둔 채 다른 푯말을 누르면 바로 옮겨간다.** 5개 의견을 견주는 것이 이 서비스의 핵심이므로 닫고 여는 과정이 없어야 한다

**말풍선이 옆 칸을 덮는 것은 문제가 아니다.** 게임에서 대사창이 화면을 덮는 것과 같은 관습이다.

**방향 자동 결정 로직이 필요하다.**

```
상단 2칸  → 아래로 펼침
하단 2칸  → 위로 펼침
컨디션    → 위로 펼침
팀장      → 위로 펼침 (화면 중앙까지 확장 가능)
좌측 칸   → 오른쪽으로 치우쳐 펼침
우측 칸   → 왼쪽으로 치우쳐 펼침
```

말풍선 꼬리는 항상 해당 캐릭터를 가리킨다.

**말풍선 본문은 일반 폰트를 쓴다**(원칙 3). 100~180자를 도트체로 읽는 것은 부담이다.

---

## 6. 로딩 연출

### 6.1 순차 등장 — 실제 응답 순서 + 최소 간격

```
API 응답 도착 → 큐에 적재 → 앞 에이전트 등장 후 최소 1000ms 경과 시 다음 등장
```

**실제 순서를 쓰되 최소 간격을 보장한다.** 정직하면서도 3~4명이 동시에 일어서서 뭉쳐 보이는 문제를 피한다.

**비용 에이전트가 항상 마지막인 것은 의도된 결과다.** 시야 출력을 받아야 판단할 수 있는 파이프라인 구조(PRD §6.2)가 화면에 그대로 드러난다. 다른 에이전트가 일하는 동안 비용은 **대기 포즈**로 있다가 시야가 끝난 뒤 타이핑을 시작한다. 사용자가 이유를 알 필요는 없지만, 보고 있으면 "저 사람은 뭔가를 기다렸구나"가 읽힌다.

컨디션 에이전트는 기타란이 없으면 호출되지 않으므로 **처음부터 앉은 상태**로 시작한다.

### 6.2 팀장 지연 대응

4명이 다 일어섰는데 결론이 아직 안 나온 경우.

| 경과 | 포즈 |
|---|---|
| 0~5초 | 서류 검토 루프 |
| 5~10초 | **"고민 중" 포즈** (기존 제작 에셋 재활용) |
| 10~20초 | 고민 포즈 유지, 미세 동작 추가 |
| 20초~ | **타임아웃 안내** |

기존에 제작된 "고민 중" 스프라이트를 지연 포즈로 재활용하면 새 에셋이 필요 없고, 포즈 변화만으로 "멈춘 게 아니라 진행 중"이 전달된다.

**20초 초과 시 타임아웃 처리:**
```
"시간이 조금 걸리고 있어요"
[ 계속 기다리기 ]  [ 다시 시도 ]
```

안내가 없으면 사용자가 새로고침해 상담 결과가 날아간다. 이때만 텍스트가 등장하며, 평소에는 진행 상태 문구를 쓰지 않는다.

### 6.3 결론 등장

팀장 작업이 끝나면 결론 단어가 팀장 바에 나타난다. 별도의 극적 연출(화면 중앙 스포트라이트 등)은 넣지 않는다. 매번 보면 성가시고, 원칙 4에 따라 결론이 푯말보다 무거워지면 안 된다.

---

## 7. 타이포그래피

### 7.1 폰트

| 용도 | 폰트 | 라이선스 |
|---|---|---|
| 도트체 | **Galmuri11 / Galmuri14** | SIL OFL 1.1 |
| 본문 | **Pretendard** | SIL OFL 1.1 |
| 대체 | Noto Sans KR | SIL OFL 1.1 |

<cite index="8-1">Galmuri는 SIL 오픈 폰트 라이선스 1.1에 따라 사용할 수 있으며, 폰트가 자체적으로 판매되지 않는 한 자유롭게 사용·연구·수정·재배포할 수 있다.</cite> <cite index="9-1">상업적 무료 글꼴이라 해도 폰트 파일 자체를 파는 행위는 금지된다.</cite>

**Galmuri 크기별 선택:** 푯말(16자)은 Galmuri11, 결론 단어와 제목은 Galmuri14. 도트 폰트는 지정된 픽셀 크기의 정수배로만 렌더링해야 뭉개지지 않는다.

### 7.2 크기 체계

```
서비스 제목      Galmuri14 × 3
결론 (가라)      Galmuri14 × 2
푯말 (16자)      Galmuri11 × 2
버튼            Galmuri11 × 2

말풍선 본문      Pretendard 15px / 행간 1.7
입력 폼 레이블   Pretendard 13px
입력값          Pretendard 15px
공연 정보·금액   Pretendard 15px (tabular-nums)
보조 설명        Pretendard 12px
```

금액과 좌석 번호에는 `font-variant-numeric: tabular-nums`를 적용해 자릿수가 흔들리지 않게 한다.

### 7.3 푯말 텍스트 처리

PRD가 최대 16자를 보장하지만 방어 코드가 필요하다. 초과 시 말줄임 대신 **폰트 크기를 한 단계 내리고**, 그래도 넘치면 말줄임한다. 도트 폰트는 크기를 소수배로 줄일 수 없으므로 ×2 → ×1.5가 아니라 Galmuri11 ×2 → Galmuri9 ×2로 전환한다.

---

## 8. 반응형

### 8.1 원칙

**정수배 스케일만 사용한다.** 브레이크포인트마다 스프라이트 배율을 바꾸고, 중간 크기는 여백으로 흡수한다.

| 폭 | 배율 | 레이아웃 |
|---|---|---|
| ≥1440px | ×3 | 기본 |
| 1024~1439px | ×2 | 기본 |
| 768~1023px | ×2 | 랜딩: 상하 분할 / 메인: 2×2 유지 |
| <768px | ×2 | §8.2 |

### 8.2 모바일

PWA 우선 전략(PRD §12.3)이므로 모바일이 실사용 환경일 가능성이 높다.

**랜딩:** 좌우 분할을 상하로 전환. 제목·좌석 조명이 위, 폼이 아래. 스포트라이트는 화면 상단에 가로로 눕힌다.

**메인:** 2×2 그리드를 유지하되 캐릭터를 작게 하고, 푯말을 캐릭터 위가 아니라 **칸 하단에 가로로** 배치한다. 말풍선은 화면 하단 고정 시트로 전환한다(게임 대사창과 동일).

**터치 대응:** 호버가 없으므로 푯말 탭이 유일한 진입점이다. 탭 영역을 푯말보다 넉넉하게(최소 44×44px) 잡는다.

---

## 9. 에셋 목록

### 9.1 캐릭터 스프라이트

| 에셋 | 프레임 | 비고 |
|---|---|---|
| 관점 에이전트 착석·타이핑 | 3~4 | 4인 공용 베이스 + 소품 레이어 |
| 관점 에이전트 착석·대기 | 2 | 비용 에이전트 대기용 |
| 관점 에이전트 기립·푯말 | 1 | 흔들림은 CSS transform |
| 관점 에이전트 착석·정지 | 1 | `is_muted` 상태 |
| 팀장 착석·대기 | 2 | |
| 팀장 착석·작업 | 3~4 | |
| 팀장 고민 중 | 2 | **제작 완료** |
| 팀장 결론 도출 | 1~2 | **제작 완료** |

**푯말 흔들림은 스프라이트 프레임이 아니라 CSS transform으로 처리한다.** 프레임으로 만들면 진폭을 점수에 따라 조절할 수 없다.

### 9.2 소품·오브젝트

캐스팅보드, 좌석배치도, 오페라글라스, 계산기, 티켓, 영수증, 증정품 상자, 폴라로이드, 헤드셋, 안내데스크, 책상, 의자, 노트북

### 9.3 UI

푯말(빈 판), 말풍선(9-slice, 방향 4종), 극장 좌석(1좌석 단위 타일), 스포트라이트(밝기 3단계), 선호 좌석 아이콘 6종, 버튼(활성/비활성/호버)

### 9.4 제작 도구

| 작업 | 도구 |
|---|---|
| 캐릭터·소품 초안 | 이미지 생성 (현행 유지) |
| 프레임 단위 애니메이션 | **Aseprite 등 도트 전용 툴** |
| 레이아웃·컴포넌트 | 직접 구현 |

**애니메이션 프레임은 이미지 생성으로 일관성을 맞추기 어렵다.** 타이핑 루프처럼 손 위치만 미세하게 다른 프레임은 도트 툴에서 직접 찍는 편이 빠르고 정확하다.

---

## 10. 라이선스 — 수익화 대비

**전제: 향후 유료화·광고·투자 유치 등 어떤 형태로든 수익이 발생해도 문제가 없어야 한다.**

### 10.1 폰트

| 폰트 | 라이선스 | 상업 이용 | 웹 임베딩 |
|---|---|---|---|
| Galmuri (전 시리즈) | SIL OFL 1.1 | ✅ | ✅ |
| Pretendard | SIL OFL 1.1 | ✅ | ✅ |
| Noto Sans KR | SIL OFL 1.1 | ✅ | ✅ |

**OFL의 유일한 제약:** 폰트 파일 자체를 판매할 수 없다. 서비스에 임베딩하는 것은 자유다.

**금지:** 라이선스가 "개인 비영리만 무료"이거나 "300인 이하 기업만" 같은 조건부인 폰트. 무료로 배포되는 한글 폰트 중 상당수가 여기 해당하므로 반드시 개별 확인한다.

**self-host 권장.** CDN이 중단되면 폰트가 깨진다. OFL은 재배포를 허용하므로 직접 호스팅해도 된다. 라이선스 전문을 함께 배포한다.

### 10.2 픽셀 아트 에셋

AI 이미지 생성으로 제작한 캐릭터는 **사용에는 문제가 없으나, 두 가지를 인지해야 한다.**

1. **생성 서비스 약관을 확인할 것.** 상업적 이용 허용 여부와 출력물 소유권 조항은 서비스마다 다르고 변경될 수 있다.
2. **AI 생성물은 저작권 보호를 받지 못할 수 있다.** 인간 창작성이 인정되지 않으면 저작권이 발생하지 않으며, 그 경우 **제3자가 그대로 가져다 써도 막을 수단이 없다.** 서비스가 성장했을 때 실질적 리스크가 된다.

**권장 대응:** 초안은 AI로 뽑되, **도트 툴에서 손으로 수정·재작업한 최종본을 사용한다.** 인간의 창작적 수정이 들어가면 그 부분에 대해 저작권 주장이 가능해진다. 애니메이션 프레임을 직접 찍기로 한 것(§9.4)이 이 목적도 겸한다.

### 10.3 사운드

현재 사양에 사운드는 없다. 추가한다면 CC0 또는 상업 이용 명시 라이선스만 사용한다.

### 10.4 PRD 측 라이선스 이슈

디자인 외 영역에서 수익화 시 문제가 될 수 있는 항목이다. **`prd.md` 부록 B에 상세를 둔다.**

- 크롤링 데이터의 데이터베이스제작자 권리
- 예매처 이용약관의 자동 수집 금지 조항
- 좌석배치도 이미지 복제 금지 (사실 정보만 추출)
- 공연 포스터·스틸 이미지 사용 금지
- 배우 초상·퍼블리시티권
- 서비스명 상표 확인
- LLM API 상업 이용 조건
- 수익화 시 이용약관·개인정보처리방침 정비

---

## 11. 미결정 항목

| 항목 | 내용 |
|---|---|
| 컨디션 부스 형태 | 가로 낮은 부스가 맞는지, 다른 배치가 나은지 |
| 푯말 흔들림 수치 | `BASE_AMP` 8px / `BASE_PERIOD` 500ms는 초안. 실물 확인 필요 |
| 말풍선 최대 폭 | 100~180자가 몇 줄로 떨어지는지 확인 후 확정 |
| 선호 좌석 아이콘 도안 | 6종을 어떻게 도식화할지 |
| 이용 내역 모달 내용 | 과거 상담 목록 표시 형식 |
| 전환 애니메이션 | 랜딩 → 메인 전환 방식 |
| 모바일 말풍선 | 하단 시트 전환 시 푯말 비교 경험을 어떻게 유지할지 |

---

## 부록 A. 구현 체크리스트

### 픽셀 아트
- [ ] `image-rendering: pixelated` 적용
- [ ] 스케일이 정수배인가 (×2 / ×3 / ×4)
- [ ] 반투명(opacity)으로 비활성 처리하고 있지 않은가 → 밝기·채도로 처리
- [ ] 도트 폰트가 지정 픽셀 크기의 정수배로 렌더링되는가

### 푯말
- [ ] `is_muted`일 때 푯말을 들지 않는가 (빈 푯말 금지)
- [ ] 흔들림 진폭이 `|axis_score|`에 비례하는가
- [ ] 최소 진폭 30%가 보장되는가
- [ ] 위상이 에이전트마다 다른가
- [ ] `prefers-reduced-motion` 시 정지하고 대체 표현이 있는가
- [ ] 푯말 텍스트 16자 초과 시 방어 로직이 있는가

### 말풍선
- [ ] 열어둔 채 다른 푯말을 누르면 옮겨가는가 (닫고 여는 과정 없음)
- [ ] 위치에 따라 펼침 방향이 자동 결정되는가
- [ ] 본문이 일반 폰트인가 (도트체 금지)

### 로딩
- [ ] 실제 응답 순서를 쓰되 최소 1초 간격이 보장되는가
- [ ] 비용 에이전트가 시야를 기다리는 동안 대기 포즈인가
- [ ] 컨디션 미호출 시 처음부터 앉은 상태인가
- [ ] 20초 초과 시 타임아웃 안내가 뜨는가

### 랜딩
- [ ] 로그인 전 폼이 숨겨지지 않고 어둡게 표시되는가
- [ ] 스포트라이트가 개수가 아니라 밝기·범위로 진행을 표현하는가
- [ ] 항목 수가 9~14개로 변해도 완성도 계산이 동작하는가
- [ ] 직전 상담 응답이 기본값으로 채워지는가

### 라이선스
- [ ] 사용 중인 모든 폰트가 상업 이용 가능한가
- [ ] 폰트 라이선스 전문이 함께 배포되는가
- [ ] 픽셀 에셋에 인간의 창작적 수정이 들어갔는가
- [ ] 조건부 무료 폰트가 섞여 있지 않은가

### PRD 정합성
- [ ] 푯말 문구가 자기 축의 언어인가 (판단 로직은 PRD 소관)
- [ ] 결론이 푯말보다 시각적으로 무거워지지 않았는가
- [ ] 축 점수 색이 푯말 바탕 전체를 칠하고 있지 않은가
