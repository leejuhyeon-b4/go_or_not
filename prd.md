# 「이 회차 갈까말까」 PRD v2.0

> **문서 범위**
> 서비스 정의, 에이전트 역할 분담, 입출력 계약, 데이터 구조, 판단 알고리즘을 규정한다.
> **화면 설계·UI·인터랙션·시각 디자인은 이 문서의 범위가 아니며 `design.md`에 위임한다.**
> 단, 에이전트 출력의 **데이터 계약**(푯말 문자열 길이, 필드 구성 등)은 화면이 의존하므로 여기서 정의한다.

| 항목 | 내용 |
|---|---|
| 서비스명 | 이 회차 갈까말까 (가제) |
| 버전 | **v2.0** — 케이스 스터디 6건 반영 개정판 |
| 작성일 | 2026-08-16 |
| 대상 장르 | **뮤지컬·연극 전용** (영화 제외) |
| 전제 | 사용자는 **이미 티켓을 확보한 상태**로 진입한다 |
| 부속 문서 | `test_cases.md` (회귀 테스트 픽스처), `design.md` (예정) |

### v1.0 대비 주요 변경

실제 관람 케이스 6건을 시스템에 돌려 검증한 결과를 반영했다. 결론 정확도는 6건 중 4건 완전 일치, 1건 방향 일치·최선책 누락, 1건 오답이었다. **오답 원인 분석 결과 알고리즘 구조 문제는 0건이었고, 전부 도메인 사실 오류·데이터 부재·계산 규칙 오류였다.**

- **온보딩 폐지** — 전 항목을 매 상담으로 이동
- **이벤트 크롤링 폐지** — 6건 중 성공 0건. 사용자 직접 입력으로 전환
- **이벤트 점수 = 끌림도 × 희소성** 승수 구조로 변경
- **`works` / `seasons` 2단 스키마** — 시즌제 대응
- **나눔(SHARING)** 도입
- **취소 수수료 자동 계산**
- **축 가중치 도입** — 덕심·비용 상향, 이벤트 하향
- **컨디션 `defer` 상태** — 판단 유보하고 사용자에게 반환
- **원칙 6 신설** — 검증된 사실이 아니면 말하지 않는다

---

## 1. 서비스 정의

### 1.1 해결하는 문제

뮤지컬·연극 관객은 티켓을 확보한 뒤에도 "이 회차를 실제로 갈지"를 두고 고민한다. 고민의 축은 여러 개이며 서로 충돌한다. 애배가 나오지만 자리가 나쁘고, 증정이 좋지만 몸이 힘들고, 자리는 좋지만 가격이 부담된다. 사람은 이 축들을 동시에 저울질하기 어렵고, 대개 하나의 축에 꽂혀 나머지를 놓친다.

이 서비스는 **각 축을 전담하는 에이전트를 병렬로 세워 놓친 변수를 드러내고, 팀장 에이전트가 최종 결론을 낸다.**

### 1.2 판단 단위

**단 하나의 (회차 × 좌석 × 지불액) 조합.**

- 작품 전체에 대한 판단이 아니다. "이 극을 보지 마라"는 결론은 존재하지 않는다.
- "가지 마라"는 **"이 회차를 가지 마라"**를 뜻하며, 사용자가 다른 회차를 관람하는 것은 관심 밖이다.
- **대안 회차 탐색은 제공하지 않는다** (§11.1).

### 1.3 최종 결론

팀장은 **`가라` / `가지마라` 2택**으로 결론을 낸다. 중간값·유보는 없다.

단, 결론보다 **5개 푯말이 화면에 남는 것**이 사용자 가치의 핵심이다. 사용자가 결론에 동의하지 않더라도 놓친 축을 확인했다면 서비스는 목적을 달성한 것이다.

`가지마라`일 때 팀장은 `alternative` 필드로 나눔·취소를 제안할 수 있다.

### 1.4 팀장 성향

**중립.** 과소비를 말리지도, 관람을 부추기지도 않는다. 축 신호를 규칙대로 취합한다.

### 1.5 타겟 사용자

**이미 뮤지컬·연극을 많이 본 사람들(연뮤덕).** 이 전제가 시야 판단 기준을 결정한다(§5.2). 라이트 관객 기준(뒤에서 전체를 봐야 한다)을 적용하면 실제 사용자와 어긋난다.

청소년 할인 대상 사용자도 상당수 존재한다. 다만 학업·업무 충돌은 다른 부담과 동일하게 중립 처리하며, 특별 가중치를 두지 않는다.

---

## 2. 핵심 설계 원칙

구현 중 판단이 필요할 때 이 원칙으로 돌아온다.

### 원칙 1 — 어떤 관점 에이전트도 "가지 마라"를 말하지 않는다

각 에이전트는 자기 축의 신호(-2 ~ +2)만 출력한다. 종합 판단은 팀장 전담이다.

> *배경:* 초기 설계에서 비용·컨디션이 브레이크, 덕심·이벤트가 액셀 역할을 하며 2:2 고정 대립이 발생했다. 각 에이전트가 "찬성/반대"가 아니라 "내 축에서 몇 점"만 말하면 대립 구도가 소멸한다.

### 원칙 2 — 화면에 표시한 정보는 시스템이 다시 계산하지 않는다

"폴라로이드 **랜덤** 증정"이라고 보여주고 끌림도를 받았다면, 시스템이 랜덤 확률을 다시 곱해서는 안 된다. 사용자의 답에 이미 반영되어 있다.

이 원칙 위반이 설계 과정에서 반복 발견된 이중 계산의 원인이었다. 배부 방식, 적립 배수, 촬영 허용 여부, 이벤트 희소성 모두 동일하게 적용된다.

### 원칙 3 — 통신은 "해석"이 필요할 때만 쓴다

> **판단 기준: 원본 사실이 필요하면 공통 번들, 다른 에이전트의 *해석*이 필요하면 통신.**

이벤트와 덕심이 서로에게 필요한 것은 "오늘 촬영 허용", "애배 출연" 같은 원본이며, 공통 번들에 이미 존재한다.

**시야 → 비용만 예외인 이유:** 비용 에이전트가 필요한 것은 원본이 아니다. "R석 15만 원"만으로는 비쌌는지 알 수 없고, "이 자리가 실제로 R석값을 하는가"라는 **판단**이 필요하다. 시야 에이전트만 생성할 수 있는 값이다.

**통신이 필요해지는 세 조건**(에이전트 간 합의 필요 / 한 출력이 다른 에이전트의 탐색 범위를 변경 / 다중 라운드 수렴 필요) 중 이 구조에 해당하는 것은 없다. 새 축 추가 시 이 세 조건으로 판정할 것.

#### 축 간 상관은 통신이 아니라 `primary_drivers`로 푼다

축이 독립이어도 **같은 원인으로 함께 움직이는** 경우가 있다. 애배 미출연 시 덕심이 떨어지고 동시에 이벤트(무대인사·사인회·애배 폴라)도 떨어진다. 팀장이 이를 독립된 두 부정 신호로 세면 하나의 원인을 두 번 세는 것이 된다.

**통신으로는 풀리지 않는다.** 두 에이전트가 대화해도 "누가 양보할 것인가"를 정할 규칙이 여전히 필요하며, 호출 비용만 증가한다.

해결: 각 에이전트가 **자기 점수를 만든 원인**을 함께 출력하고, 팀장이 대조해 중복을 할인한다(§6.3, §7.2).

### 원칙 4 — 축 침범 금지, 단 자기 축은 정직하게

한 에이전트가 **다른 축에 속한 이점**을 자기 점수에 가산하면 이중 계산이다.
- 전관행사의 할인율 → 비용 축. 이벤트 에이전트는 무시
- 티켓 손실 위험 → 비용 축. 덕심·컨디션은 언급하지 않음

**단, 자기 축에 속한 것은 정직하게 채점한다.** 촬영 허용은 덕심 축에서도 실제 가치가 있으므로 덕심이 점수를 낸다. 겹치는 부분은 `primary_drivers` 대조로 팀장이 할인한다. **에이전트가 스스로 점수를 낮추는 방식은 축을 오염시키므로 쓰지 않는다.**

**서술은 자유롭다.** 덕심이 "본진이 나오고 촬영까지 허용되는 회차"라고 쓰는 것은 권장된다. 점수만 자기 축에서 나오면 된다.

### 원칙 5 — 사용자가 더 잘 아는 것은 묻거나 넘긴다

증정 품목의 가치, 이벤트 희소성, 양도 가능성, 배우 동선의 유불리는 시스템 추정보다 사용자 판단이 정확하다.

대신 **사용자가 계산할 수 없는 것**(개인 내 정규화, 좌석 물리 특성, 취소 수수료, 축 간 상관)에 에이전트 역량을 집중한다.

**시스템이 알 수 없으면 판단하지 않고 사용자에게 되돌린다.** 컨디션의 `defer` 상태가 이 원칙의 구현이다(§5.5).

### 원칙 6 — 검증된 사실이 아니면 말하지 않는다

케이스 스터디에서 발견된 오답 6건이 전부 이 원칙 위반이었다. 전 시즌 정보로 이번 시즌을 추론하고, 확인하지 않은 연출 정보로 좋은 자리를 깎아내리고, 전관행사에 할인이 있다고 가정했다.

- 작품별 연출 메모에는 **검증된 사실만** 넣는다
- 데이터가 없으면 `missing_info`에 기록하고 confidence를 낮춘다
- **틀린 확신은 침묵보다 나쁘다**

### 원칙 7 — 입력 항목 추가는 비용이다

항목 하나마다 이탈률이 오른다. 필수는 최소로, 나머지는 기타란으로.

**직전 상담 응답을 기본값으로 제시**해 반복 입력 부담을 줄인다(§3.4).

---

## 3. 사용자 입력 명세

### 3.1 온보딩 — 없음

회원가입 후 바로 상담으로 진입한다.

> *배경:* v1.0에서 선호 좌석·거주 지역·애배 목록을 온보딩으로 받으려 했으나, ① 선호 좌석은 회차마다 달라지고 ② 거주 지역은 거부감을 유발하며 ③ 애배는 매 상담의 "오늘 출연?" 문항과 중복이었다. 진입 장벽이 0이 되는 것은 니치 서비스에 유리하다.
>
> 로그인의 가치는 관람 이력 축적과 개인 내 정규화에 남으며, 이는 사용자 입력 없이 사용하다 보면 쌓인다.

### 3.2 매 상담 — 필수 (9항목)

#### R-1. 공연 제목
검색·선택 → 내부 `season_id`에 매핑. **작품이 아니라 시즌 단위로 매핑한다**(§8.5).

#### R-2. 관람 일시·회차
날짜 + 시각 + 낮공/밤공. 상담 시각도 함께 기록한다(취소 수수료 계산용).

> *근거:* 상담 시점이 예매 시점이 아니라 공연 당일인 경우가 많다.

#### R-3. 좌석 위치
`층 / 블럭 / 열 / 번`. 극장별 좌석 체계에 따라 입력 폼이 달라진다.

**좌석 등급·정가·할인율은 묻지 않는다.** 좌석배치도와 정가 테이블에서 자동 산출한다(§9.2).

> *근거 (CASE 5):* 51,000원을 S석 정가로 오판했다. 실제로는 R석 30% 할인이었다. 이는 입력 부족이 아니라 DB 부재로 인한 오류이며, 사용자에게 물어서 해결할 문제가 아니다.

#### R-4. 티켓값

```
① 직접 예매 (용병표·교환표 포함)
     └ 실제 지불액
② 양도받음
     └ 실제 지불액
     └ 추가 결제 있으면 그 금액
```

**범위 밖:** 자격 증빙 권종(장애인·국가유공자·수험생·청소년)의 증빙 리스크는 판단하지 않는다. 사용자 책임 영역이며 이 커뮤니티 사용자는 숙지하고 있다.

#### R-5. 오늘 캐스팅에 본진·애배 출연?
`출연함(누구)` / `출연 안 함` / `해당 없음`. 페어 조합까지 받으면 정확도가 올라간다.

> *근거:* "애배가 있는가"가 아니라 "오늘 나오는가"를 묻는다. 애배가 있어도 오늘 안 나오면 덕심 점수가 완전히 달라진다.

#### R-6. 이 작품 처음 보시나요?
`자첫` / `봤음(몇 회차)`

**덕심·시야·이벤트 3축에 영향을 준다.**

| | 덕심 | 시야 | 이벤트 |
|---|---|---|---|
| **자첫** | 작품 경험 자체가 + | 무대 전체 조망 비중 ↑ | 재관람 혜택·적립 가치 ↓ |
| **회전** | 캐스팅 희소성이 전부 | 배우 관찰 비중 ↑ | 적립 가치 ↑ |

관람 이력이 DB에 있으면 자동으로 채워진다.

#### R-7. 선호 좌석 1·2순위

6개 중 두 개를 순위로 선택.

| 값 | 라벨 | 설명 |
|---|---|---|
| `FRONT` | 무조건 앞 | 표정·숨소리. 목 아픈 건 감수 |
| `CENTER` | 중블 사수 | 열은 타협해도 중앙 블럭은 포기 못 함 |
| `WHOLE` | 한눈에 | 무대 전체 조망 |
| `ACTOR_PATH` | 애배 동선 | 사이드여도 최애 쪽·등퇴장 통로 우선 |
| `AISLE` | 통로석 | 객석 이벤트 접근, 드나들기 편함 |
| `VALUE` | 가성비 | 등급 대비 체감만 괜찮으면 위치 무관 |

**가중치: 1순위 ×1.0, 2순위 ×0.5**

2순위까지 받는 이유는 선호 이유가 갈리는 자리를 조합으로 구분하기 위함이다.
- `FRONT` + `AISLE` → 애배 근접형
- `WHOLE` + `AISLE` → 쾌적한 시야형

**`VALUE`가 포함되면 위치 선호를 등급 내 상대 위치로 해석한다**(§5.2).

#### R-8. 오늘 오페라글라스 쓰실 생각인가요?
`쓸 예정` / `안 씀`

> *근거 (CASE 6):* A석 최저가를 잡되 그중 최앞중앙을 고르고 오글을 지참하는 행동 패턴이 확인되었다. 좌석이 앞이어도 오글을 드는 경우가 있으므로 조건부가 아니라 매번 묻는다.
>
> **오글은 거리를 보정하지 각도는 보정하지 못한다.** 2층 정수리뷰는 확대해도 정수리뷰다(§5.2).

#### R-9. 안 가기로 하면 뭘 할 수 있나요? *(복수 선택)*

| 값 | 라벨 | 손실 |
|---|---|---|
| `NO_TRANSFER` | 양도 안 나갈 것 같다 | 실지불액 |
| `SELF_DISCOUNT` | 자할해야 양도 가능 | 자할 폭 |
| `TRANSFERABLE` | 양도 가능 | 0 |
| `NEEDS_PROOF` | 증빙 도움 주며 양도 가능 | 0 + 마찰 |
| `SHARING` | 나눔 가능 (혜택 받고 자리만 넘김) | 실지불액, **단 혜택은 확보** |

**복수 선택이다.** `NO_TRANSFER`와 `SHARING`은 동시에 성립한다. 양도가 안 되어도 나눔은 가능하다.

> *근거 (CASE 6):* 양도 불가 티켓이었으나 지인에게 나눔하여 재관람 혜택을 확보했다. 단일 선택으로 두면 이 최선책을 놓친다.

취소 가능 여부와 수수료는 R-2·R-4로 자동 계산하므로 묻지 않는다(§5.3).

### 3.3 매 상담 — 조건부 (이벤트)

#### 1단계 — 유무
> **오늘 이벤트가 있나요?** `있음` / `없음`

`없음` → 이벤트 에이전트 미호출, 회색 푯말 표시

#### 2단계 — 종류 *(복수 선택)*
```
☐ 커튼콜 촬영 허용      ☐ 사인회
☐ 스페셜 커튼콜         ☐ 증정
☐ 무대인사              ☐ 럭키드로우
☐ 관객과의 대화(GV)     ☐ 적립 혜택
☐ 직접 입력 ___________
```

**전관행사와 포토타임은 선택지에서 제외했다.** 전관은 그 자체로 아무것도 주지 않으며, 혜택이 있으면 그 혜택을 고르면 된다. 남겨두면 "전관 + 커튼콜 촬영"을 둘 다 골라 이중 계산이 발생한다.

**"직접 입력"이 중요하다.** 새 이벤트 유형은 계속 생기며, 자유 입력에 쌓이는 값을 보면 어떤 선택지를 추가해야 할지 데이터로 알 수 있다.

#### 3단계 — 세부 *(해당 항목만 펼침, 전부 선택 입력)*

**증정**
```
품목      ___________  (자유 입력)
배부방식  ○ 전원 증정   ○ 전원 지정   ○ 전원 랜덤
```

| 배부 방식 | 의미 | 애배 개념 | 중복 |
|---|---|---|---|
| 전원 증정 | 종류가 하나, 모두 같은 것 | 없음 | **발생함** |
| 전원 지정 | 여러 종류 중 선택 가능 | 있음 (선택) | 배우 다르면 없음 |
| 전원 랜덤 | 여러 종류 중 무작위 | 있음 (운) | 매번 다른 배우 |

**시스템은 중복을 감점하지 않는다.** 회전러라도 또 갖고 싶을 수 있으며, 사용자가 끌림도에 반영한다.

**품목명은 자유 입력이며 이벤트 에이전트가 정규화한다.** "폴라"·"폴라로이드"·"폴카"를 같은 것으로 알아듣는 것은 LLM이 잘하는 일이므로 별도 파서가 필요 없다. 저장은 원문과 정규화 값을 모두 남긴다.

**품목별 가치 테이블은 두지 않는다.** 새 품목이 나와도 코드를 고치지 않으며, 개인차는 끌림도가 흡수한다.

**스페셜 커튼콜** → 참여 배우·넘버 (선택 입력)
**적립 혜택** → `더블` / `트리플` / `쿼드`
**럭키드로우** → 세부 없음 (현장 공개)

#### 4단계 — 끌림도
> **오늘 [선택한 이벤트]가 있네요. 얼마나 끌리세요?**

| 라벨 | 값 |
|---|---|
| 쓸모없음 | 0 |
| 별로 | 1 |
| 보통 | 2 |
| 끌림 | 3 |
| 가고싶음 | 4 |

복수 선택 시에도 **하나로 묶어 한 번만** 묻는다. 사용자는 전체를 종합해 판단한다.

#### 5단계 — 희소성
> **오늘 못 가면 이 이벤트를 다시 만날 기회가 있나요?**
> `많다` / `적다` / `없다` / `모르겠음`

"다른 회차에도 있나요?"라는 객관적 사실을 묻지 않는 이유: 이번 주 내내 있어도 사용자가 이미 다른 회차를 예매했을 수 있다. **"내가 다시 만날 수 있나"로 물으면 사용자가 자기 예매 현황을 반영한다.**

`모르겠음`은 정보 부재이므로 confidence를 낮춘다.

### 3.4 매 상담 — 선택 (1항목)

#### S-1. 기타란

```
▸ 그 밖에 고민되는 부분이 있나요? (선택)

  부대비용(KTX·숙소·촬영 장비 등), 컨디션,
  전날·당일·다음날 일정, 막차 시간 등
  자유롭게 적어주세요
```

**부대비용은 별도 항목이 아니라 기타란에 통합한다.** 금액은 비용 에이전트가 자연어에서 추출한다. 사용자가 어디에 무엇을 적을지 고민할 필요가 없어진다.

**기타란은 공통 번들에 포함되어 모든 에이전트가 읽는다.** 컨디션 에이전트만 기타란이 비어 있을 때 호출되지 않는다.

### 3.5 입력 부담 완화

필수 9항목은 적지 않다. **직전 상담 응답을 기본값으로 제시**해 실질 탭 수를 줄인다. 선호 좌석·오글 의향은 회차마다 바뀔 수 있지만 자주 바뀌지는 않으므로, 그대로면 넘기고 다를 때만 수정한다. 화면 구현은 `design.md`.

### 3.6 관람 후 피드백

| 항목 | 형태 |
|---|---|
| 실제 결과 | **`갔음` / `나눔` / `안 감`** |
| 자리 만족도 | 최악/별로/보통/좋음/최고 (0~4) — **`갔음`일 때만** |
| 증정 만족도 | 동일 5단계 — `갔음` 또는 `나눔`일 때 |

**`나눔`은 "간 것"도 "안 간 것"도 아닌 제3의 상태다.** 혜택은 본인이 받았지만 자리는 경험하지 않았으므로 **자리 만족도를 수집하면 데이터가 오염된다.**

**텍스트 후기는 받지 않는다.** 사용자는 이미 다른 서비스나 커뮤니티에 후기를 남겼을 가능성이 높으며, 중복 작성 요구는 이탈로 이어진다.

*활용:*
- 자리 만족도 → 극장×블럭 통계 축적(§9.1)
- 사전 끌림도 vs 사후 증정 만족도 → 개인 응답 편향 추출
- 실제 결과 → 팀장 가중치 튜닝

---

## 4. 에이전트 개요

| # | 이름 | 담당 축 | 점수 | 가중치 | 특수 |
|---|---|---|---|---|---|
| 1 | **덕심** | 캐스팅·회차 희소성 | -2~+2 | **1.2** | — |
| 2 | **시야** | 좌석 위치·시야 | -2~+2 | 1.0 | → 비용에 전달 |
| 3 | **비용** | 실지출·손실 | -2~+2 | **1.2** | 시야 출력 수신 |
| 4 | **이벤트** | 증정·이벤트 | -2~+2 | **0.8** | — |
| 5 | **컨디션** | 체력·일정 실행가능성 | -2~+2 | 1.0 | **거부권·`defer`** |
| — | **팀장** | 취합·최종 결론 | — | — | 하이브리드 |

### 가중치 근거

케이스 스터디 6건에서 **이벤트가 단독으로 결론을 만든 적이 한 번도 없었다.** 반면 덕심과 비용은 거의 매번 결정적이었다. 사용자 진술("비용과 덕심이 목소리가 커야 한다")과도 일치한다.

초기값이며 피드백 축적 후 튜닝한다.

---

## 5. 에이전트 상세 명세

### 5.1 덕심 에이전트

**한 줄 정의:** 오늘 이 캐스팅이 나에게 얼마나 특별한가.

#### 입력
- 오늘 캐스팅 (R-5)
- 관람 이력: 이 작품 관람 횟수, 이 페어 관람 횟수 (R-6)
- **남은 시즌 내 애배 출연 회차 수** (캐스팅보드에서 자동 계산)
- 회차 성격: 첫공 / 막공 / 프리뷰 / 트라이아웃 / 일반
- `photo_allowed`, 이벤트 정보
- 기타란 원문

#### 판단 기준

| 요소 | 방향 |
|---|---|
| 애배 출연 | 강한 + |
| 선호 페어 조합 | + |
| 이 페어 첫 관람 | + (여러 번 본 페어는 특별도 하락) |
| 첫공 / 막공 | 강한 + (대체불가) |
| 프리뷰 / 트라이아웃 | + |
| **남은 애배 회차 적음** | + |
| 자첫 | + (작품 경험 자체) |
| 애배 미출연 | 0 (마이너스 아님) |

**관람 횟수는 같은 축 내부의 계산이다.** 여러 번 본 페어면 특별도가 낮아지는데, 이는 브레이크가 아니라 그냥 낮은 점수다.

> *포화도 3분할:* ① 이 페어를 몇 번 봤나 → 덕심 ② 최근 관람 빈도·피로 → 컨디션 ③ ~~예산 소진율~~ → 예산을 묻지 않으므로 제거

#### 배역 비중은 판단하지 않는다

> *근거 (CASE 2):* "2인극이라 애배 분량이 많다"를 점수 근거로 제시했으나 기각되었다. 배역 비중은 변수가 너무 많다. **기타란에 언급이 있을 때만 참조한다.**

#### 촬영 허용 처리
`photo_allowed = true` + 애배 출연 → **점수 가산 + `irreplaceable`**. `primary_drivers`에 `PHOTO_ALLOWED`와 `FAVORITE_ACTOR`를 기록하고, **중복 할인은 팀장이 처리한다.**

#### 하지 않는 것
- 관람 여부 권고
- 대안 회차 제시
- 좌석·비용·체력 판단 (서술에서 언급은 가능)

---

### 5.2 시야 에이전트

**한 줄 정의:** 이 자리가 오늘 나의 목적에 얼마나 맞는가.

#### 입력
- 좌석 위치 (R-3) + 좌석배치도 데이터 (등급·통로 위치·블럭 경계)
- 선호 좌석 1·2순위 (R-7)
- **오글 사용 의향 (R-8)**
- 자첫 여부 (R-6)
- 애배 출연 여부 (R-5)
- 이벤트 종류, `photo_allowed`
- 극장 물리 스펙 + 기하 계산 (§9.1)
- 시야제한석 명단
- 작품별 연출 메모 (검증된 사실만)
- 축적 자리 만족도 통계

#### 기준선 — 연뮤덕 기준

**v1.0의 "작품 감상 명당 = 중앙 7~15열"은 라이트 관객 기준이었다.**

| | v1.0 | **v2.0** |
|---|---|---|
| 작품 감상 명당 | 중앙 7~15열 | **중앙 5~12열** |
| 앞열(1~6열) | 가깝지만 전체 조망 아쉬움 | **기본적으로 좋은 자리** |
| 뒤·2층 | 전체 조망 유리 | 특정 연출에 한해서만 우위 |
| 기조 | 중립 | **가까운 쪽 선호를 기본 가정** |

> *근거 (CASE 3):* 알라딘 1층 6열에서 매직 카펫 씬이 잘 보였다. "스펙터클 극은 뒤가 유리"라는 통설은 1회 관람 전제에서 나온 것이며, 회전하는 사용자에게는 맞지 않는다.

#### 3중 평가

**같은 좌석이 목적에 따라 A가 되기도 D가 되기도 한다.**

| 기준 | 명당 | 적용 조건 |
|---|---|---|
| **작품 감상** | 중앙 5~12열, 무대 전체 조망 | 항상, 자첫이면 비중 ↑ |
| **배우 관찰** | 1~3열, 애배 동선 쪽 사이드 포함 | 애배 출연 시, 회전이면 비중 ↑ |
| **이벤트·촬영** | 정면·중앙 | `photo_allowed`, 무대인사·GV |

비중은 R-7 선호 좌석이 결정한다.

#### 등급 내 상대 위치 (`VALUE` 포함 시)

선호에 `VALUE`가 포함되면 **위치 선호를 절대 좌표가 아니라 등급 내 상대 위치로 해석한다.**

> *근거 (CASE 6):* "무조건 전진"인 사용자가 비용 때문에 A석 최저가를 잡되 그중 최앞중앙을 선택한다. `FRONT` + `VALUE` 조합이면 "S석 중 가장 앞"이 무조건 전진의 실현이며, 절대 좌표로 판정하면 -2가 나온다.

#### 오페라글라스 처리

```
오글 사용 의향 있음
  → 거리 감점 완화
  → 각도 문제(정수리뷰·극사이드 가림)는 그대로
```

**오글은 거리를 보정하지 각도는 보정하지 못한다.** 2층에서 내려다보는 구도는 확대해도 내려다보는 구도다.

#### 방향 우열은 판단하지 않는다

**위치는 사실로만 서술하고, 좋고 나쁨은 판정하지 않는다.**

```
❌ "촬영엔 살짝 오른쪽이라 각도가 아쉽습니다"   (감점)
⭕ "6열 중오블입니다"                        (사실)
```

> *근거 (CASE 3):* 누구를 찍느냐에 따라 좋은 자리가 달라지고, 스페셜 커튼콜도 동선에 따라 왼블이 유리할 수 있다. 시스템이 알 수 없는 변수가 너무 많다.
>
> R-7의 `ACTOR_PATH` 선호가 이 역할을 대신한다. 시야는 "이 자리가 무대 어느 쪽인지"만 알려주고, 사용자가 자기 애배 동선과 맞춰 본다.

**단, "중앙에서 얼마나 벗어났나"는 판단한다 [2026-09-01].** *왼블이 오른블보다 낫다*는 안 매기지만, *이 자리가 정면에서 얼마나 치우쳤나*는 좌석 위치 문제라 본다(아래 사이드 감점의 연속판). `classifySide` 가 사이드 블럭 좌석마다 `lean`(0=중앙 통로 쪽 끝, 1=벽 쪽 끝)을 내고, `agentSiya` 가 `EDGE`/`SIDE`로 못박히지 않은 좌석도 `lean` 에 비례해 완만히 감점(`lean≥0.3`일 때 `-0.6·lean`, `FRONT`/`ACTOR_PATH` 면 0)하고 위치를 서술한다("좌측 블럭이지만 중앙 통로 쪽 안쪽이라 치우침은 크지 않습니다"). 블럭 위치를 알면 "좌우 어느 블럭이 유리한지는 판단하지 않았습니다" 문구는 뺀다.

#### 사이드 감점 [2026-08-27 구현 완료 · 2026-08-31 데이터 경로 갱신]

> 구현: **`venues.base_geometry`** (관리자 도구가 격자/메모로 입력 → Supabase, `is_estimate=false`) + **`seasons.side_seats`** (수동 극싸/사이드 명단 — 그 층에 하나라도 있으면 명단 밖 좌석은 일반) · `data.js` `classifySide`/`sideZoneFor` → `resolveSeat.side_zone` · `engine.js` `agentSiya` 사이드 감점 블록. `axis_score`가 소수로 나올 수 있다(§7.1). `data/seed.js` 는 이제 폴백·테스트 픽스처 전용, `GON_SEATMAPS`(season_seat_maps) 오버레이는 미사용(§8.4).

**"좌/우 우열 판단 안 함"과는 별개다.** 앞의 것은 *같은 깊이에서 왼블이 나은지 오른블이 나은지* — 연출에 달렸으니 판단 안 함. 이건 *열의 끝쪽 좌석은 무대 옆이 잘린다*는 좌석 위치 문제.

`EDGE`/`SIDE`는 이 위치 문제를 **강제 감점으로 못박은 양 끝**이고, 그 사이 구간(사이드 블럭 안쪽)은 `lean` 연속값으로 완만히 반영한다(위 §"중앙에서 얼마나 벗어났나"). `seasons.side_seats` 명단이 있어 명단 밖이 "일반"으로 처리돼도 `lean` 은 블럭 기하에서 계속 나온다 — 명단은 강제 감점 여부만 정하지, 위치 서술까지 끄지 않는다.

**사이드 블럭 한 열을 통로석부터 세어 세 구간으로 자른다 (겹침 없는 분할, 사석=시야제한석 제외하고 셈). 경계가 모호하면 더 센 쪽(`EDGE`) 우선.**

극싸·벽싸는 `EDGE` 한 등급으로 통일. 블럭 폭에 따라 경계가 달라진다:

| 블럭 폭 | 일반 (`0`) | `SIDE` (`-0.5`) | `EDGE` (`-1.0`) |
|---|---|---|---|
| 3석 (좁은 박스) | 1~2번째 | — | 끝 1석 |
| 4석 (좁은 박스) | 1~2번째 | 3번째 | 끝 1석 |
| 5석 이상 | 1~3번째 | 4번째 ~ 끝에서 3번째 | 끝 2석 |
| 1~2석 (초소형 박스) | — | — | 전부 |

- **좁은 박스(≤4석):** 일반 = 통로 쪽 2석, `EDGE` = 벽 쪽 끝 1석, 나머지(4석일 때 3번째)가 `SIDE`.
- **5석 이상:** 일반 = 통로 쪽 3석, `EDGE` = 벽 쪽 끝 2석, 그 사이가 전부 `SIDE`.
- 5석 블럭은 `SIDE` 구간이 비어(1~3=일반, 4~5=`EDGE`) `SIDE` 없이 넘어간다.

**선호 좌석(R-7)에 따른 예외:**
| 선호 | 사이드 감점 |
|---|---|
| `FRONT` (무조건 앞/전진) | **적용 안 함** (0). 앞을 최우선으로 고른 사람에게 사이드 감점은 어긋난다 |
| `ACTOR_PATH` (애배 동선) | **`EDGE` → `-0.5`로 완화**, `SIDE` → 0. 동선 보러 사이드 감수한 사람이라 일반 사이드는 안 깎고, 무대 옆이 잘리는 끝 2석도 `-1.0` 대신 `-0.5`로 낮춘다 |
| `VALUE` (가성비) + **극싸 위치 때문에 등급·가격이 낮아진 경우** | 감점 안 함 (0). 위치를 값과 바꾼 선택을 두 번 벌하지 않는다 |
| 그 외 선호 / 선호 없음 | 위 기본 감점 그대로 (`CENTER` 선호 아니어도 적용) |

- `EDGE -1.0`은 실점이다 — `B열 앞`이라 거리 `+1`이어도 `EDGE`면 합산 `0`, `SIDE`면 `+0.5`. 구현 시 `fit → Math.round` 경로가 아니라 `score` 부동소수 보정으로 넣고 마지막에 `clamp`.
- 극장 베이스 추정이든 공연 좌배도 확정이든 감점 폭은 위 표 그대로 (예전 "`WALL`은 좌배도 있으면 `-1`" 규칙은 폐기). 베이스만 쓴 불확실성은 `⚠️`로만 표시.
- 서술은 사실부터: `1층 B열 12번, 좌측 사이드 블럭 벽 쪽 끝자리입니다. 무대 오른쪽 안쪽이 상당히 잘릴 수 있습니다.`

**폭별 예시 (s1 = 통로 쪽, 선호 없음 기준):**

| 블럭 폭 | s1 | s2 | s3 | s4 | s5 | s6 | s7 |
|---|---|---|---|---|---|---|---|
| 3석 | 0 | 0 | **E** | | | | |
| 4석 | 0 | 0 | S | **E** | | | |
| 5석 | 0 | 0 | 0 | **E** | **E** | | |
| 6석 | 0 | 0 | 0 | S | **E** | **E** | |
| 7석 | 0 | 0 | 0 | S | S | **E** | **E** |

(`E` = `EDGE -1.0`, `S` = `SIDE -0.5`, `0` = 일반. 모든 폭에서 구간이 겹치지 않는다.)

#### 세부 판단 요소
- 무대 가림: 단차, 난간, 기둥, 오케스트라 피트, 음향 부스
- 거리 → 표정 식별 가능성 (오글 의향 반영)
- 층·단차 → 앞사람 머리 가림 확률
- **통로 인접 여부** (좌석배치도에서 확정)
- 객석 이벤트·통로 등퇴장 유무 (통로석 평가에 필수)
- 내한 공연 시 자막기 위치

#### 출력 추가 필드
```json
"seat_value_grade": {
  "paid_grade": "R",
  "felt_grade": "S_HIGH",
  "gap": -1
}
```
비용 에이전트에게 전달된다 (유일한 에이전트 간 의존).

#### 초기 정확도 안전장치
- confidence를 낮게 시작
- 데이터 부족 극장·블럭에서는 말풍선 판정 문구 끝에 **경고 이모지 `⚠️`** ("확실하지 않음"의 축약 표기, design.md §5.4)
- **이 공연 좌석배치도가 아직 갱신 안 됐고 극장 기본 베이스로만 판단했으면 반드시 `⚠️`** (§9.1 레이어 2 폴백)
- **기하 계산은 보조 지표로 5~10% 비중만** — 등급 내 좌석 수가 많아 물리 계산만으로는 변별력 부족

---

### 5.3 비용 에이전트

**한 줄 정의:** 이 자리에 이 돈을 낸 것이 합당한가, 그리고 안 가면 무엇을 잃는가.

#### 입력
- 실지불액 (R-4)
- **좌석 등급·정가 (좌석배치도 + 정가 테이블에서 자동 산출)**
- `seat_value_grade` (시야 에이전트 출력)
- 처분 가능 옵션 (R-9, 복수)
- **자동 계산된 취소 수수료**
- 기타란 원문 (부대비용 추출)

#### 판단 기준

**① 지불 대비 좌석 가치**
`seat_value_grade.gap`과 실지불액을 결합. 할인율은 정가 대비 자동 산출한다.

**② 손실 계산**

| R-9 | 손실 |
|---|---|
| `NO_TRANSFER` | 실지불액 |
| `SELF_DISCOUNT` | 자할 폭 |
| `TRANSFERABLE` | 0 → **비용 축 중립. 스스로 물러남** |
| `NEEDS_PROOF` | 0 + 마찰 (소폭 감점) |
| `SHARING` | 실지불액, **단 재관람 혜택·증정은 확보** |

**⚠ `NO_TRANSFER`를 무조건 관람 쪽 가중으로 처리하지 말 것.**

> *근거 (CASE 6):* 17,500원 티켓이 양도 불가였으나, 손실 절대액이 작아 오히려 "포기해도 된다"는 근거가 되었다. **손실 금액에 따라 방향이 갈린다.** 손실 절대액과 해당 사용자의 지불 이력을 함께 봐야 한다.

**③ 취소 수수료 자동 계산**

공연일과 현재 시각으로 산출하며 사용자에게 묻지 않는다.

```
남은 일수 = 공연일 - 지금
  D-10 이상   →  장당 4,000원 (티켓 금액 10% 한도)
  D-9 ~ D-7   →  10%
  D-6 ~ D-3   →  20%
  D-2 ~ D-1   →  30%
  전날 17시 이후 →  취소 불가
  예매 당일 자정 이전 →  수수료 없음 (예매수수료도 환불)
```

**양도받은 표는 취소할 수 없다.** 취소 권한은 원 예매자 계정에만 있다. R-4의 취득 경로가 처분 가능 옵션을 제한한다.

규정은 예매처·공연마다 다르므로 `seasons.cancellation_policy`에 크롤링해 저장한다.

**④ 총 실지출**
기타란에서 부대비용을 추출해 합산한다.

#### 할인권 증정 처리

**할인권·쿠폰팩 증정은 오늘 지출 계산에 넣지 않는다.** 2만 원 할인권을 받아도 오늘 낸 금액은 그대로다. 오늘 손에 들어오지만 오늘 가치가 실현되지 않는 유일한 품목이다.

#### 하지 않는 것
- 양도 시세·취켓 가능성 판단
- 자격 증빙 권종 리스크 경고
- 예산 소진율 (예산을 묻지 않음)
- 전관행사 할인을 이벤트 축에서 재계산

---

### 5.4 이벤트 에이전트

**한 줄 정의:** 사용자 응답을 축 신호로 변환하고, 다른 축이 알아야 할 사실을 번역한다.

> **역할 주의**
> 이 에이전트의 프롬프트는 "이벤트 가치를 평가하라"가 **아니다.** 사용자가 끌림도를 직접 답하고 배우 매개형은 덕심 축과 겹치므로 평가자 역할은 축소되었다.

#### 3대 역할

**① 정규화** — 끌림도의 개인 내 상대값 계산. 사용자가 스스로 할 수 없는 유일한 계산(§6.4)
**② 번역** — `photo_allowed` → 덕심·시야 / 선착순 → 컨디션 (공통 번들 경유)
**③ 품목명 정규화** — 자유 입력 품목의 표기 통일

> *v1.0의 "발견" 역할은 삭제되었다.* 이벤트 크롤링을 폐기하고 사용자 직접 입력으로 전환했기 때문이다(§9.3).

#### 점수 계산 — 승수 구조

**희소성은 승수지 가산항이 아니다.**

```
score = 끌림도 × 희소성 승수
```

| 희소성 | 승수 |
|---|---|
| 많다 | ×0.5 |
| 적다 | ×0.8 |
| 없다 | ×1.0 |
| 모르겠음 | ×0.7 + confidence 하락 |

| 끌림도 | 기회 많다 | 적다 | 없다 |
|---|---|---|---|
| 쓸모없음 | 0 | 0 | **0** |
| 별로 | 0 | 0 | **0** |
| 보통 | 0 | +1 | +1 |
| 끌림 | +1 | +1 | +2 |
| 가고싶음 | +2 | +2 | +2 · `irreplaceable` |

> *근거 (CASE 1·2):* 끌림도 `별로`인데 희소성 `없다`를 보고 +1을 부여했다. **관심 없는 이벤트를 다시 못 만나는 것은 아무것도 잃는 것이 아니다.**

#### `irreplaceable` 3단계

정보 시점에 따라 갈린다.

| 유형 | 예 | 대체불가 |
|---|---|---|
| **확정형** | 스페셜 커튼콜(배우·넘버 사전 공지), 커튼콜 촬영, 무대인사, GV, 증정 종류, 전원 지정 | 가능 |
| **부분 확률형** | 전원 랜덤 배부 | 가능하되 약하게 |
| **완전 확률형** | 럭키드로우, 사인회 당첨 | **불가** |

**조건: 끌림도 3 이상 AND 희소성 `없다` AND 확정형**

> *근거 (CASE 2):* "스페셜 커튼콜은 사전에 알 수 없다"고 기술했으나, 실제로는 배우와 넘버가 사전 공지된다. 사인회(현장 확인)와 정반대 성격이다.

#### 이벤트 분류

**배우 매개형** — 애배 미출연 시 가치 급락. 덕심 축과 중첩되므로 `primary_drivers` 기록 필수

| 유형 | 좌석 의존 |
|---|---|
| 폴라로이드 (배우 사진) | 없음 |
| 사인회 | 없음 |
| 무대인사 | 높음 |
| 관객과의 대화(GV) | 중 |
| 촬영 허용 | 높음 |
| 스페셜 커튼콜 | 중 |

**작품·물건형** — 애배와 무관: OST·대본집, 엽서·스티커·포스터, 할인권·쿠폰팩, 적립 혜택, 커튼콜데이(작품 기록)

#### 적립 혜택

**더블·트리플·쿼드 적립일 때만 별도 레이어로 처리한다.** 회전 계획이 없으면 사용자가 `쓸모없음`으로 응답하므로 회전 여부를 따로 묻지 않는다.

#### 표기 규칙

**할인권 증정은 액면가로 표기하지 않는다.** "2만 원 할인권"이 아니라 **"재관람 할인권"**. 액면가를 강조하면 소진하지 못할 가능성이 큰 것을 현금처럼 보이게 만든다.

#### ⚠ 설계 주의사항

재관람 할인권·적립 혜택은 **쓰려면 또 가야 하므로 장기적으로 지출을 늘린다.** 이를 사용자에게 경고하지는 않는다(원칙 1 위반). 다만 **이 서비스가 의도치 않게 과소비를 유도할 수 있는 유일한 지점**이므로 이벤트 점수 튜닝 시 주의할 것.

---

### 5.5 컨디션 에이전트

**한 줄 정의:** 오늘 이 회차를 물리적으로 감당할 수 있는가.

#### 발동 조건

**기타란(S-1)이 비어 있으면 호출하지 않는다.**

기타란이 있으면 호출하되, 체력·일정 관련 내용이 없으면 `not_applicable`을 반환하고 회색 푯말로 표시한다.

#### 특수성 — 유일한 자연어 단독 판단 에이전트

다른 넷은 구조화된 입력을 받지만 **컨디션만 기타란 원문을 읽는다.** 가장 정교한 프롬프트가 필요하다.

#### 입력
- 기타란 원문
- 관람 일시·회차, 러닝타임, 현재 시각
- 극장 위치
- 최근 관람 빈도
- 선착순 이벤트 여부

#### 판단 기준

**컨디션도 플러스를 낼 수 있다.** "낮공, 집에서 20분, 다음날 휴일"이면 +2다. 브레이크 전용이 아니다.

- 이동 시간, 귀가 가능성
- 전날·당일·다음날 일정
- 최근 관람 빈도로 인한 피로 누적
- 사용자가 명시한 건강 상태

**학업·업무 충돌은 다른 부담과 동일하게 처리한다.** 특별 가중치를 두지 않는다.

#### 거부권

**발동 조건:**

1. 귀가 수단 없음 (사용자가 명시한 경우)
2. 사용자가 명시한 필수 일정과 충돌
3. 사용자가 입력한 건강 이상
4. **부담 요소 3개 이상 누적** — 단일 요소가 결정적이지 않아도 검토

**해석 규칙:**

> "평소에도 흔하다" 같은 완화 표현이 **역접("~하지만") 뒤에 놓였다면 완화가 아니라 대비로 읽는다.**

> *근거 (CASE 5, 결론 오답):* "평소 밤공 보고도 늦은 밤 귀가는 흔하**지만** 청소도 해야 하고 다음날 아침 체력 소모 약속 있음"에서 앞 절을 감당 가능 근거로 삼았다. 실제로는 뒤 절을 강조하기 위한 대비였다. **컨디션은 누적으로 작동하는 축이며, 각각이 결정적이지 않다는 이유로 넘기면 안 된다.**

거부권 발동 시 다른 축과 무관하게 팀장 결론이 `가지마라`로 확정된다.

#### `defer` — 판단 유보

**부담 요소가 있으나 감당 가능성을 판단할 근거가 없으면 판단하지 않고 사용자에게 되돌린다.**

```json
{ "axis_score": 0, "veto": false, "defer": true,
  "placard": "자습 건은 직접 판단하세요",
  "detail": "자습을 빠지셔야 하는데, 그게 감당할 만한지는 본인이
             가장 잘 아실 부분입니다. 이 부분만 따로 한 번 더
             생각해보시고 결정하세요." }
```

**판단하지 않는 것도 유효한 출력이다**(원칙 5).

#### 하지 않는 것
- 대안 회차 제시
- 귀가 수단·일정을 되묻기 (기타란에 없으면 판단하지 않음)

---

## 6. 파이프라인 및 취합 알고리즘

### 6.1 공통 입력 번들

```json
{
  "season": { "id", "work_title", "season_label", "venue_id",
              "running_time", "has_intermission", "close_date" },
  "session": { "date", "time", "matinee_or_evening",
               "is_opening", "is_closing", "now" },
  "seat": { "floor", "block", "row", "number",
            "grade",           // 좌석배치도에서 자동
            "is_aisle",        // 좌석배치도에서 자동
            "is_restricted" },
  "payment": { "acquisition": "DIRECT | TRANSFER",
               "total_paid", "surcharge",
               "list_price",      // 정가 테이블에서 자동
               "discount_rate",   // 자동 산출
               "cancellation_fee" // 자동 계산
             },
  "disposal_options": ["NO_TRANSFER", "SHARING", ...],
  "casting": { "today_cast", "has_favorite_actor",
               "favorite_actors_today",
               "remaining_favorite_sessions" },
  "first_watch": bool,
  "seat_preference": { "first", "second" },
  "opera_glass": bool,
  "events": [ { "type", "item", "distribution",
                "participating_actors", "photo_allowed",
                "is_actor_mediated", "certainty_class",
                "user_appeal", "user_scarcity" } ],
  "history": { "this_work_count", "this_pair_count", "recent_30d_count" },
  "free_text": string | null
}
```

### 6.2 실행 순서

```
                  공통 입력 번들
                        │
        ┌───────┬───────┼───────┬────────────┐
        ▼       ▼       ▼       ▼            ▼
      덕심     시야   이벤트  [컨디션]      (1단계 병렬)
                │
                │ seat_value_grade
                ▼
              비용                          (2단계)
                │
                ▼
              팀장                          (3단계)
```

- **에이전트 간 통신은 시야 → 비용 하나뿐.**
- API 호출: 상담 1건당 **5~6회**
- 예상 지연: 8~15초

#### 고정 파이프라인 채택

**오케스트레이터-워커 패턴**이며, 축 분담은 설계 시점에 고정된다. 팀장이 런타임에 축을 배분하는 디스패처 방식은 기각했다.

| 검토안 | 얻는 것 | 잃는 것 |
|---|---|---|
| 팀장 디스패처 | 불필요한 호출 제거, 확장 용이 | 팀장 호출 2회, 지연 3~5초, 푯말 개수 변동 |

조건부 호출(이벤트 유무, 기타란 유무)은 하드코딩으로 처리하며, **해당 없는 에이전트도 회색 푯말로 자리를 지킨다**(§7.3).

### 6.3 팀장 — 하이브리드 구조

팀장은 **종합 판단자**이며 규칙 엔진이 아니다. 절대 어겨서는 안 되는 것만 코드로 강제한다.

| 층 | 담당 | 내용 |
|---|---|---|
| **가드레일** | 코드 | 거부권, 결격 감지, 드라이버 중복, 가중치 조정 |
| **판단** | LLM | 가드레일 안에서 종합, 결론, 설명문, RAG 해석 |

가드레일은 결론을 대체하는 것이 아니라 **팀장 프롬프트에 입력 제약으로 주입**된다.

#### 가드레일 (코드)

```
STEP 1. 거부권
  컨디션.veto == true
    → 결론 = 가지마라 (확정). 팀장은 설명문만 생성

STEP 2. defer 처리
  컨디션.defer == true
    → 해당 부담 요소를 점수에 반영하지 않음
    → 팀장 설명문에 "직접 판단 필요" 항목으로 명시

STEP 3. 드라이버 중복 감지
  2개 이상 에이전트의 primary_drivers에 동일 항목 존재
    → 두 번째 축 이후의 기여를 할인
    → 팀장 프롬프트에 "같은 원인에서 나옴"으로 주입

STEP 4. 결격 감지
  axis_score == -2  → 결격 플래그, 팀장 프롬프트에 주입
    (단순 합산으로 뭉개지 않도록 경고)

STEP 5. 손실 기반 가중치 조정
  disposal_options에 TRANSFERABLE 포함 → 비용 축 하향 (중립화)
  NO_TRANSFER만 존재
    → 손실 절대액 확인
       금액 큼 → 비용 축 상향
       금액 작음 → 상향하지 않음

STEP 6. 나눔 검토 플래그
  disposal_options에 SHARING 포함
  AND 이벤트에 재관람 혜택·적립 포함
    → alternative_candidate = SHARING

STEP 7. 축 가중치 적용
  덕심 1.2 / 비용 1.2 / 시야 1.0 / 이벤트 0.8 / 컨디션 1.0

STEP 8. 정규화 (§6.4)
```

#### 판단 (LLM)

- 결론 `가라` / `가지마라` 2택
- 대체불가 요소가 있고 결격이 없으면 관람 쪽으로 기움
- 축 신호 상충 시 무게 판단
- **`가지마라`이고 `alternative_candidate`가 있으면 나눔 제안**
- RAG 유사 케이스 참조
- 결론 설명문 작성
- confidence 낮으면 "정보가 부족합니다" 표기

#### 나눔 우선 검토 규칙

> **재관람 혜택·적립이 걸린 회차에서 `가지마라`가 나오면 나눔을 반드시 검토한다.**

적립이 있는데 그냥 안 가면 **돈도 잃고 적립도 잃는다.** 나눔하면 혜택은 챙기고 자리만 넘기므로 이 조건에서는 거의 언제나 우위다.

> *근거 (CASE 6):* 시스템이 `가지마라`를 냈으나 나눔을 제안하지 않았다. 실제 사용자는 지인에게 나눔하여 재관람 혜택을 확보했다. 이것이 케이스 스터디 최대 오답이었다.

### 6.4 개인 내 정규화

상담하는 사용자는 대체로 마음이 들떠 모든 값을 최대치로 답하는 경향이 있다.

```
normalized = (raw - user_mean) / user_stddev
```

**3~4회 상담 누적 후 작동.** 그 전에는 원점수를 쓰되 confidence를 낮춘다.

적용 대상: 이벤트 끌림도, 자리 만족도, 증정 만족도. 로직은 하나만 구현해 공용한다.

---

## 7. 에이전트 출력 계약

### 7.1 공통 스키마

```json
{
  "agent": "DEOKSIM | SIYA | COST | EVENT | CONDITION",
  "axis_score": -2 ~ +2,       // 대개 정수. 시야는 사이드 감점(§5.2)으로 -0.5 단위 소수 가능
  "primary_drivers": ["ENUM", ...],
  "confidence": 0.0 ~ 1.0,
  "irreplaceable": bool,
  "veto": bool,              // 컨디션만
  "defer": bool,             // 컨디션만
  "not_applicable": bool,
  "is_muted": bool,          // 회색 푯말 여부
  "placard": "최대 16자",
  "detail": "100~180자 한 문단",
  "missing_info": ["항목명", ...]
}
```

**시야 추가 필드:** `seat_value_grade`

#### 척도를 -2~+2로 정한 이유
0~100점 스케일은 LLM 응답의 일관성이 낮다(동일 조건에 72점과 65점을 오감). 5단계 정수는 안정적이며 RAG 비교가 가능하다.

### 7.2 `primary_drivers` 열거값

축 간 상관을 팀장이 감지하려면 **드라이버 명칭이 에이전트 간에 동일해야 한다.** 자유 문자열은 대조가 불가능하므로 열거형으로 고정한다.

| 값 | 의미 | 주로 내는 에이전트 |
|---|---|---|
| `FAVORITE_ACTOR` | 애배 출연 | 덕심, 이벤트 |
| `PAIR_RARITY` | 페어 조합 희소성 | 덕심 |
| `OPENING_CLOSING` | 첫공·막공 | 덕심 |
| `REMAINING_SESSIONS` | 남은 애배 회차 적음 | 덕심 |
| `FIRST_WATCH` | 자첫 | 덕심 |
| `PHOTO_ALLOWED` | 촬영 허용 | 덕심, 시야, 이벤트 |
| `EVENT_APPEAL` | 사용자 끌림도 | 이벤트 |
| `EVENT_SCARCITY` | 이벤트 희소성 | 이벤트 |
| `SEAT_QUALITY` | 좌석 자체의 시야 | 시야 |
| `SEAT_PREFERENCE_FIT` | 선호 좌석 부합 | 시야 |
| `PRICE_GAP` | 지불액 대비 좌석 가치 | 비용 |
| `LOSS_RISK` | 안 갈 때의 손실 | 비용 |
| `CANCELLATION_FEE` | 취소 수수료 | 비용 |
| `TRAVEL_BURDEN` | 이동·귀가 부담 | 컨디션 |
| `SCHEDULE_CONFLICT` | 일정 충돌 | 컨디션 |
| `FATIGUE` | 피로 누적 | 컨디션 |

새 드라이버 추가 시 이 표와 프롬프트 열거값을 함께 갱신한다.

### 7.3 푯말 데이터 계약

각 에이전트는 결론을 팀장에게 전달한 뒤 화면에 **한 줄 푯말**로 표시된다. 커서를 올리거나 누르면 `detail`이 펼쳐진다.

**PRD가 규정하는 것:**
- `placard` 최대 **16자** (실측 7~15자. 20자로 두면 LLM이 늘려 씀)
- `detail` **100~180자**, 한 문단 (실측 108~152자)
- 푯말은 자기 축의 언어로만. "가지 마세요" 금지
- 시야는 데이터 부족 시 "확실하지 않음" 표기
- **`is_muted = true`인 회색 푯말은 `detail`을 생략한다**

**회색 푯말 (A안 채택):** 해당 없는 에이전트도 자리를 지킨다. "오늘은 이벤트가 없어요", "특이사항 없음" 등. 5개 푯말이 나란히 제시되는 것이 이 서비스의 산출물 형태이므로 개수 변동을 최소화한다.

**design.md에 위임:**
- 푯말(=말풍선) 강도 표현 방식(색·굵기 등), 애니메이션
- 호버·탭 인터랙션
- 5개 푯말 배치, 로딩 중 순차 등장
- 회색 푯말의 시각 처리
- 결론과 푯말의 위계
- 좌석 입력 UI (극장별 체계 차이 대응)
- 선호 좌석 시각적 선택
- 직전 응답 기본값 제시 방식

---

## 8. 데이터 스키마

### 8.1 사용자

```sql
users (
  user_id PK, created_at, auth_provider
)

user_stats (                          -- 온보딩 없음. 사용하며 축적
  user_id FK,
  appeal_mean, appeal_stddev,
  satisfaction_mean, satisfaction_stddev,
  last_seat_pref_1st, last_seat_pref_2nd,   -- 기본값 제시용
  last_opera_glass
)
```

### 8.2 상담

```sql
consultations (
  consultation_id PK, user_id FK, season_id FK,
  session_datetime, consulted_at,
  seat_floor, seat_block, seat_row, seat_number,
  acquisition, total_paid, surcharge,
  first_watch,
  seat_pref_1st, seat_pref_2nd,
  opera_glass,
  has_favorite_actor_today,
  free_text,
  final_verdict,          -- GO | NO_GO
  alternative,            -- SHARING | CANCEL | null
  team_lead_reasoning
)

consultation_disposal_options (       -- 복수 선택
  consultation_id FK, option
)

consultation_events (
  consultation_id FK,
  event_type, item_raw, item_normalized,
  distribution, participating_actors,
  photo_allowed, is_actor_mediated, certainty_class,
  user_appeal, user_scarcity
)

agent_outputs (
  consultation_id FK, agent,
  axis_score, primary_drivers JSON,
  confidence, irreplaceable, veto, defer,
  not_applicable, is_muted,
  placard, detail, missing_info JSON,
  raw_response JSON
)
```

### 8.3 피드백

```sql
outcomes (
  consultation_id FK,
  result ENUM('ATTENDED','SHARED','SKIPPED'),
  seat_satisfaction 0-4,      -- ATTENDED만
  event_satisfaction 0-4,     -- ATTENDED, SHARED
  responded_at
)
```

### 8.4 극장·좌석

```sql
theaters (
  venue_id PK, name, address, lat, lng,
  nearest_station, last_transit_time
)

theater_specs (
  venue_id FK, floor,
  stage_to_row1_distance, row_count, row_spacing,
  tier_start_row, has_orchestra_pit, balcony_overhang_row,
  aisle_positions JSON,        -- 통로 위치. CASE 1·2에서 유효성 검증됨
  obstruction_notes JSON
)

seat_blocks (
  venue_id FK, floor, block_name, seat_range, center_offset
)

-- [2026-08-27 신설] 극장 기본 베이스 — 좌배도 없이도 항상 있는 표준 배치
venue_base_geometry (
  venue_id FK, floor,
  block_ranges JSON,          -- 블럭별 통로 위치 + 벽 위치. side 구간(§5.2)은
                              -- seat_range·aisle 위치·블럭 폭에서 파생 (≤4석 vs 5석+). 예:
                              -- [{block:'OL', seat_range:[1,20], wall_side:'left', aisle_side:'right'}]
  proscenium_cut_note,        -- 아치 가림에 대한 일반 서술
  is_estimate BOOL DEFAULT true
)

-- [2026-08-27 신설] 공연별 좌석배치도 오버레이 — 갱신되면 base_geometry를 덮는다
-- [2026-08-31 갱신] 이 별도 테이블은 미사용. 관리자 도구가 아래처럼 바꿈:
--   블록 기하 → venues.base_geometry (관리자가 격자/메모로 직접 입력, is_estimate=false)
--   등급/통로/장애인석/극싸사이드/고속도로 → seasons.{seat_grades,aisle_seats,wheelchair_seats,side_seats,cross_aisles}
--   시야제한석 → seat_grades 의 "시야제한" 등급 (정가가 따로라서). restricted_seats 는 레거시.
season_seat_maps (
  season_id FK, floor,
  block_boundaries JSON,      -- 실측 블럭 경계
  restricted_seats JSON,      -- [레거시] → 이제 "시야제한" 등급
  side_overrides JSON,        -- → seasons.side_seats
  updated_at,
  source
)
```

**폴백 규칙 [2026-08-31 갱신]:** 관리자가 좌석배치도를 입력하면 `venues.base_geometry.is_estimate=false` 가 되고, `seasons.side_seats` 명단이 그 층에 있으면 그걸 신뢰(명단 밖 = 일반). 둘 다 없으면 극장 기하 추정으로 판단하고 `⚠️`. 감점 폭(§5.2 표)은 동일.

**구현 형태:** `venue.base_geometry = { is_estimate, note, floors:{ <floor>:[ { name, side, seat_min, seat_max, aisle_end:'min'|'max'|null, wall_end:'min'|'max'|null, row_from?, row_to?, aliases:[] } ] } }` (중앙 블럭은 `wall_end:null`, 앞열 부채꼴이면 `row_from`/`row_to` 로 열범위별 블럭 세트). `data.js classifySide()`가 좌석의 `zone`/`block` 표기 또는 좌석번호로 블럭을 찾아 `dWall`·`dAisle`·`width`를 재고 `sideZoneFor()`로 `EDGE`/`SIDE`/`null` 을 낸다. `window.GON_SEATMAPS` 오버레이 자리는 코드에 남아 있으나 항상 빈 결과.

### 8.5 작품 — 2단 구조

> **구현 [2026-08-31]:** 아래 정규화 스케치 대신 `data/schema.sql` 은 이걸 **`seasons` 한 테이블로 평탄화**
> (`works` 없음 — `work_title` 텍스트 컬럼, `season_prices`→`prices jsonb`, `season_seat_grades`→`seat_grades jsonb`,
> `season_restricted_seats`→"시야제한" 등급, 통로/장애인석/극싸사이드/고속도로 각 jsonb). 관리자 "기본정보" 탭이
> `work_title`·`season_label`·`venue_id`·`open_date`·`close_date`·`prices` 를 손입력한다.

```sql
works (
  work_id PK, title, genre
)

seasons (
  season_id PK, work_id FK,
  season_label,              -- "2026"
  venue_id FK,
  open_date, close_date,
  run_number,                -- 초연/재연/삼연
  running_time, has_intermission,
  cancellation_policy JSON
)

season_castings (
  season_id FK, session_datetime, role, actor_name
)

season_prices (
  season_id FK, grade, list_price
)

season_seat_grades (          -- 좌석 → 등급 매핑
  season_id FK, floor, block, row, seat_range, grade
)

season_restricted_seats (
  season_id FK, floor, block, row, seat_numbers JSON,
  source_url, crawled_at
)

season_staging_notes (        -- 검증된 사실만
  season_id FK,
  has_revolving_stage,
  has_aisle_event,
  has_audience_interaction,
  subtitle_screen_position,
  verified_notes TEXT
)
```

#### 시즌 단위 vs 작품 단위

| 판정 | 기준 |
|---|---|
| 자첫 여부 | **작품 단위** — 2022에 봤으면 2026은 자첫 아님 |
| 페어 관람 횟수 | **시즌 단위** — 시즌 다르면 페어가 존재하지 않음 |
| 재관람 할인 자격 | **작품 단위** — 여러 시즌 티켓을 모두 인정하는 경우가 많음 |

> *근거 (CASE 1):* 2022년 초연 시즌 기사를 근거로 2026 시즌 이벤트를 추론해 오답을 냈다. 시즌마다 극장·캐스팅·연출·가격·이벤트가 전부 다르다.

### 8.6 축적 통계 (RAG 소스)

```sql
seat_satisfaction_stats (
  venue_id, floor, block, row_range, grade,
  sample_count, avg_satisfaction, updated_at
)

anonymized_patterns (
  pattern_id PK,
  genre, venue_type, seat_grade, seat_zone,
  price_band, had_favorite_actor, first_watch,
  had_event, event_certainty_class,
  agent_scores JSON, final_verdict,
  result, satisfaction,
  embedding VECTOR
)
```

**개인 식별 정보는 `anonymized_patterns`에 포함하지 않는다.**

---

## 9. 데이터 소스 정책

### 9.1 시야 데이터 — 4레이어

**전제:** 씨야 등 UGC 시야 공유 서비스는 **크롤링하지 않는다.**

*근거:* ① 후기는 작성자 개인 저작물 ② 동종 서비스이며 무단 수집이 알려질 경우 니치 팬덤 특성상 평판 손실이 서비스 가치를 초과 ③ 로그인 기반 UGC로 약관 위반 가능성.

**수집 대상을 UGC가 아닌 사실 데이터로 한정한다.**

**레이어 1 — 극장 물리 스펙 (1회성 수동 구축)**
20개 주요 극장 × (무대~1열 거리, 층별 열 수, 열 간격, 단차 시작 열, 2층 돌출, 오케피, 난간·기둥). 극장 공식 홈페이지 대관 안내에 공개되어 있다.

**레이어 2 — 좌석배치도 (크롤링, 공연 오픈 시 1회)**
열·번호 체계, **통로 위치**, 블럭 경계, 좌석→등급 매핑. side 구간(`EDGE`/`SIDE`/그 외, §5.2)은 통로·벽 위치에서 파생한다. 예매처·제작사 SNS·포털에 공개된 사실 정보.

> CASE 1·2에서 좌석배치도 기반 통로 판정이 실제와 일치함을 확인했다.

**[2026-08-27 신설] 극장 베이스 + 공연 오버레이 2단 구조.** 레이어 1의 극장 물리 스펙에 **표준 블럭 배치(`venue_base_geometry`)를 항상 붙여 둔다** — 좌배도 크롤링 전이라도 "이 블럭 끝 2석은 무대 옆이 잘린다"는 극장 구조상 이미 안다. 공연이 오픈되어 `season_seat_maps`가 갱신되면 그것이 베이스를 덮는다. **미갱신 상태면 극장 베이스로 판단하고 `⚠️`를 띄운다**(§5.2, §8.4). 감점 폭은 베이스든 오버레이든 동일.

**레이어 3 — 작품별 연출 메모 (수동, 병목)**
시즌당 3~5줄. 회전무대, 객석 이벤트 통로, 자막기 위치. **검증된 사실만 기록한다**(원칙 6).

**레이어 4 — 축적 자리 만족도**
관람 후 1탭(0~4). 극장×블럭 단위 집계. **나눔 회차는 제외**(§3.6).

**보조 — 기하 계산 엔진 (규칙, LLM 아님)**
좌석 좌표 → 거리·수평각·앙각. 결정론적이라 환각이 없다. **최종 판단 비중 5~10%로 제한.**

### 9.2 크롤링 대상

**모두 공연 오픈 시 1회 수집으로 충분한 정적 데이터다.**

| 대상 | 주기 | 출처 |
|---|---|---|
| 좌석배치도·통로 | 오픈 시 1회 | 예매처, 제작사 SNS, 포털 |
| 좌석→등급 매핑 | 오픈 시 1회 | 예매처 |
| 등급별 정가 | 오픈 시 1회 | 예매처 |
| 시야제한석 명단 | 오픈 시 1회 | 예매처 공지 |
| 취소 수수료 규정 | 오픈 시 1회 | 예매처 공지 |
| 캐스팅보드 | 1일 1회 | 예매처 |

**예매처:** NOL(인터파크), YES24 티켓, 티켓링크, 멜론티켓. 좌석 체계와 공지 형식이 다르므로 **파서를 예매처별로 분리**한다.

**해시 비교로 증분 처리한다.** 내용이 동일하면 파싱을 건너뛴다.

**상담 시점에는 크롤링하지 않는다. DB 조회만 한다.** 상담 시 크롤링하면 ① 응답 시간 초과 ② 반복 호출로 차단 ③ 크롤링 실패가 상담 실패로 전파된다.

### 9.3 이벤트 — 크롤링 폐기

**케이스 스터디 6건에서 이벤트 크롤링 성공률 0%였다.**

실패 원인:
- 회차 단위 이벤트가 제작사 SNS에만 있고 검색에 잡히지 않음
- 검색 시 전 시즌 정보가 먼저 노출되어 시즌 혼동 발생
- 나무위키 등 통합 문서는 시즌 구분이 사실상 불가능

**사용자 직접 입력으로 전환한다**(§3.3). 사용자는 이미 어떤 이벤트인지 알고 있으며, 시스템보다 정확하다.

부수 효과: 제작사 SNS 크롤러, LLM 이벤트 파서, 시즌 매칭 로직이 전부 불필요해진다. Phase 2 개발 부담이 크게 감소한다.

### 9.4 사용자 직접 입력

좌석 위치, 실지불액, 애배 출연 여부, 자첫 여부, 선호 좌석, 오글 의향, 처분 가능 옵션, **이벤트 전체**, 기타란.

### 9.5 애배력 공유 인프라

| 층 | 처리 |
|---|---|
| works·seasons·극장·좌석배치도·캐스팅·정가 | **공유** |
| 크롤러·파서 | **공유** |
| 사용자 계정·상담 기록·관람 이력 | **분리** |

**개인 데이터 분리 이유:** 두 서비스의 이용약관과 수집 목적이 다르다. 갈까말까는 "상담 데이터 익명화 활용" 동의를 받지만 애배력 사용자는 해당 동의를 한 적이 없다. 계정 연동이 필요해지면 이후 SSO로 연결하되 DB는 처음부터 분리한다.

**API 키와 Workspace는 서비스별로 분리한다.**

---

## 10. RAG 및 피드백 루프

### 10.1 흐름

```
1. 상담 → consultations + agent_outputs 저장
2. 공연일 이후 알림 → 피드백 (갔음/나눔/안 감, 자리·증정 만족)
3. outcomes 업데이트
4. 익명화 → anonymized_patterns + 임베딩
5. 새 상담 시 유사 케이스 검색 → 팀장 프롬프트에 주입
```

**초기 시드:** `test_cases.md`의 6건을 시드 데이터로 투입한다.

### 10.2 활용처

| 데이터 | 개선 대상 |
|---|---|
| 자리 만족도 | 시야 정확도 |
| 사전 끌림도 vs 사후 증정 만족도 | 개인 응답 편향 보정 |
| 실제 결과 | 팀장 축 가중치 튜닝 |
| 축 조합 vs 만족도 | 팀장 임계값 조정 |
| 이벤트 자유 입력 누적 | 선택지 추가 판단 |

### 10.3 개인정보

- 익명화 패턴만 타 사용자 추천에 재사용
- 개인 식별 정보는 별도 테이블 분리
- 이용약관 고지·동의 필요
- 피드백 요청 빈도 과다는 이탈 유발

### 10.4 모델 학습 없음

API 모델은 호출이 쌓여도 가중치가 변하지 않는다. "경험이 쌓일수록 정확해지는" 효과는 전적으로 RAG와 통계 축적으로 구현한다. 프로바이더를 교체해도 축적분은 유지되나, 임베딩 모델 교체 시 벡터 재계산이 필요하다.

---

## 11. 범위 밖

### 11.1 대안 회차 탐색 — 명시적 제외

- 전 회차의 잔여석·캐스팅·가격을 알아야 하며 "사용자 직접 입력" 방침과 충돌한다
- 대안 회차는 사용자가 서비스보다 잘 안다
- 판단 단위가 "이 회차"로 고정되어 있다

**대신 유지:** "이 회차만의 대체불가 요소"(`irreplaceable`).

### 11.2 커뮤니티 크롤링 — 초기 범위 밖

**대상:** 디시인사이드 뮤지컬·연극 미니갤러리/통합갤러리, 더쿠 등

**제외 근거:**
- 게시물·댓글은 작성자 개인 저작물
- 이용약관 자동 수집 금지 조항 가능성
- **이 커뮤니티가 곧 타겟 사용자층이다.** 무단 수집이 알려지면 평판 손실이 서비스 존속을 위협한다
- 배우 루머·관크 언급·특정인 지목 혼입 시 명예훼손·개인정보 문제
- 검증되지 않은 정보로 정확도가 낮음

**조건부 도입 시:** 커뮤니티는 **단서로만** 사용하고 공식 채널에서 원본 확인 후 저장한다. 커뮤니티 텍스트는 DB에 저장하지 않고 사용자에게 노출하지 않는다. 도입 전 robots.txt 및 약관 확인 필수.

**시야 데이터는 커뮤니티로 대체할 수 없다.** §9.1의 4레이어가 유효하다.

### 11.3 기타 제외 항목

| 항목 | 사유 |
|---|---|
| 영화 | 양도·권종변경·캐스팅 회차 개념 없음 → 별개 서비스 |
| 티켓 예매·양도 중개 | 티켓 확보 후 진입이 전제 |
| 양도 시세·취켓 확률 | 동일 |
| 자격 증빙 권종 리스크 경고 | 사용자 책임 |
| 프레스콜 | 대부분 관객 입장 불가 |
| 전관행사 (단독 항목) | 혜택이 없으면 일반 회차와 동일 |
| 포토타임 | 선택지에서 제외 |
| 증정 퀄리티 사전 평가 | 실물 공개가 직전이라 판단 불가 |
| 배역 비중·분량 | 변수 과다. 기타란 언급 시만 |
| 예산 관리 | 예산을 묻지 않음 |
| 좌석 방향 우열 판정 | 누구를 보느냐에 따라 달라짐 |
| 학업·업무 특별 가중치 | 다른 부담과 동일 처리 |
| 씨야 등 UGC 시야 서비스 | 저작권·약관·평판 |
| 이벤트 크롤링 | 성공률 0/6 |
| 상담 시점 실시간 크롤링 | 응답 시간·차단·장애 전파 |
| 화면·UI 설계 | **design.md 위임** |

---

## 12. 비기능 요구사항

### 12.1 API 비용

#### 상담 (사용자 데이터)
- 상담 1건당 5~6회
- **관점 에이전트는 저가 모델(Haiku 등), 팀장만 상위 모델(Sonnet 등)**
- Console Spend Limits, Workspace 분리로 예산 관리

#### 수집 파싱 (공개 정보)
공연 오픈 시 1회 + 캐스팅 일 1회. 물량이 적어 무료 티어로 충분하다.

#### ⚠ 무료 티어 사용 경계 — 반드시 준수

> **공개 정보 파싱은 무료 티어 허용, 사용자 데이터는 유료 API 전용.**

Gemini 무료 티어는 전송 내용과 응답이 제품 개선에 사용될 수 있으며 사람이 검토할 수도 있다. 유료 티어에서는 학습에 사용되지 않는다.

| 처리 대상 | 티어 |
|---|---|
| 크롤링한 공지·좌석배치도 파싱 | 무료 허용 |
| **관점 에이전트 5인** | **유료 전용** |
| **팀장** | **유료 전용** |
| **임베딩(상담 기록)** | **유료 전용** |

**구조적 방지책:** 사용자 데이터를 다루는 경로가 무료 티어 키를 주입받지 못하도록 코드 수준에서 차단한다. 환경변수로 바꿀 수 있다는 것은 실수로도 바뀔 수 있다는 뜻이다.

*참고: 2026년 4월부터 Gemini 무료 티어에서 Pro 모델이 제외되어 Flash·Flash-Lite만 사용 가능하며, 분당 5~15회 제한이 있다. 배치 작업이므로 제약이 되지 않는다.*

#### 프로바이더 전환
REST 구조이므로 종속성이 없다. **어댑터 패턴으로 구현해 환경변수로 교체 가능하게 한다.** 자동 폴백 체인은 구현·유지 비용이 절감액을 초과하므로 만들지 않는다.

### 12.2 응답 시간

목표 15초 이내. 1단계 병렬 + 2단계 + 팀장. 지연은 로딩 중 푯말 순차 등장으로 완화(design.md).

### 12.3 플랫폼

**웹(PWA) 우선.** 앱스토어 심사 없이 빠른 반영, 설치 장벽 없는 초기 사용자 확보. 이후 푸시 알림 정확도가 병목이 되면 네이티브 확장(백엔드·DB 재사용).

---

## 13. 미결정 항목 및 검증 필요 가설

### 13.1 미결정

| 항목 | 내용 |
|---|---|
| 팀장 임계값 | 가라/가지마라 경계값 |
| 결격(-2) 처리 강도 | 결론을 막을지, 감점만 할지 |
| 드라이버 중복 할인율 | 두 번째 축 이후 기여를 얼마나 깎을지 |
| **대체불가와 결격이 같은 원인일 때** | CASE 4(엘리자벳)와 CASE 5가 반대 결과. 잠정 기준: **결격의 원인이 대체불가 요소를 무력화하는가**. 케이스 부족으로 미확정 |
| `NO_TRANSFER` 손실 임계액 | 얼마부터 "큰 손실"로 볼지 |
| 가드레일/LLM 경계 | 코드 강제 범위 |
| 덕심 → 이벤트 통신 | `primary_drivers`로 상관이 부족하면 검토 |
| "극이 취향 아님" 별도 항목화 | 기타란으로 충분한지 |
| 임베딩 모델·벡터DB | 미선정 |
| 극장 초기 커버리지 | 20개 목표, 우선순위 미정 |

### 13.2 케이스 스터디로 미검증인 영역

`test_cases.md` 6건에서 시험되지 않아 배포 전 별도 검증이 필요하다.

- 취소 수수료가 결론을 바꾸는 케이스 (D-3 이내 20~30% 구간)
- 회전 포화 (같은 페어 3회 이상 시 덕심 점수 하락)
- 이벤트 없는 순수 회차
- 결격(-2) 2개 이상 동시 발생
- 개인 내 정규화 (3~4회 누적 후)
- 컨디션 `defer` 실동작

### 13.3 검증 필요 가설

합성 인터뷰(AI 생성 페르소나) 기반 가설이며 실제 리서치로 검증되지 않았다.

| 가설 | 상태 |
|---|---|
| 사용자는 이미 갈 마음이 있고 정당화를 원한다 | **반증됨** |
| 할인권종 사고 방지가 킬러 기능이다 | **반증됨** |
| 상담 시점이 공연 당일인 경우가 많다 | 미검증, 설계 반영 |
| 부대비용이 티켓값을 넘는 케이스가 있다 | 미검증 |

**합성 인터뷰는 실제 사용자 리서치를 대체하지 않는다.** 배포 전 최소 5명의 실사용자 인터뷰를 권장한다. 현재 케이스 스터디 6건은 모두 사용자 1인의 이력이므로 개인 성향이 강하게 반영되어 있다.

---

## 14. 개발 우선순위

### Phase 1 — 판단 엔진
- 공통 입력 번들 스키마 확정
- **5개 에이전트 프롬프트 작성 + 출력 스키마 검증** ← 최우선
  - 축 침범 없이 자기 관점만 말하는지
  - `primary_drivers` 열거값 준수
  - 푯말 16자 / detail 100~180자 준수
- 팀장 가드레일(코드) + 판단(LLM) 구현
- 극장 물리 스펙 5개 + 좌석배치도 5개
- 시즌별 연출 메모 5개
- **`test_cases.md` 6건으로 회귀 테스트**

### Phase 2 — 데이터 파이프라인
- 예매처별 파서 4종 (좌석배치도·등급·정가·시야제한석·취소규정·캐스팅)
- 해시 증분 처리
- 애배력과 공유 DB 층 분리
- 극장 20개 / 시즌 10개로 확장

### Phase 3 — 축적 루프
- 로그인·관람 이력
- 피드백 수집 알림 (갔음/나눔/안 감 3택)
- 개인 내 정규화
- RAG 검색 + 팀장 컨텍스트 주입

### Phase 4 — 튜닝
- 축 가중치·임계값 조정
- 시야 정확도 개선
- 이벤트 점수 튜닝 (§5.4 주의사항)
- 이벤트 자유 입력 분석 → 선택지 추가

---

## 부록 B. 라이선스·법적 이슈 — 수익화 대비

**전제: 향후 유료화·광고·투자 유치 등 어떤 형태로든 수익이 발생해도 문제가 없어야 한다.** 무료 서비스일 때 넘어가던 것들이 수익화 시점에 리스크가 된다.

> 이 부록은 법률 자문이 아니다. 실제 수익화 전 변호사 검토를 권장한다.

### B-1. 크롤링 데이터

**사실 정보 자체는 저작권 대상이 아니다.** 좌석 번호, 통로 위치, 등급별 가격, 캐스팅 명단, 시야제한석 목록, 취소 규정은 모두 사실이므로 이를 추출해 자체 DB를 구축하는 것은 저작권 침해가 아니다.

**다만 세 가지를 구분해야 한다.**

| 대상 | 성격 | 대응 |
|---|---|---|
| 사실 정보 (가격·좌석번호·캐스팅) | 저작권 대상 아님 | 추출·저장 가능 |
| **좌석배치도 이미지 자체** | 도형저작물성 인정 가능 | **이미지를 복제하지 말 것.** 정보만 추출해 자체 도면 제작 |
| **DB 전체의 상당 부분** | 데이터베이스제작자의 권리 | 상당 부분 복제·전송은 침해 소지 |

**한국 저작권법은 데이터베이스제작자의 권리를 별도로 보호한다.** 상당한 투자로 제작된 DB의 상당 부분을 복제·전송하면 개별 데이터가 사실이더라도 침해가 될 수 있다. 예매처의 공연 DB 전체를 긁어 미러링하는 형태가 되지 않도록, **서비스 운영에 필요한 범위(다루는 작품·극장)로 수집을 한정한다.**

**이용약관 위반은 저작권과 별개의 계약 문제다.** 예매처 약관에 자동 수집 금지 조항이 있으면 저작권상 문제가 없어도 계약 위반이 된다. 수익화 전 각 예매처(NOL·YES24·티켓링크·멜론티켓) 약관과 `robots.txt`를 확인한다.

**수집 예의:** 요청 간격 제한, User-Agent 명시, 공연 오픈 시 1회 원칙 준수(§9.2). 서버에 부담을 주지 않는 수준이면 분쟁 가능성이 크게 낮아진다.

### B-2. 이미지·초상

| 대상 | 가능 여부 |
|---|---|
| 공연 제목, 극장명 | ✅ 사실 정보 |
| 배우 이름 | ✅ 사실 정보 |
| **공연 포스터·스틸컷** | ❌ 제작사 저작물 |
| **배우 사진** | ❌ 저작권 + 초상권 + 퍼블리시티권 |
| **증정품 실물 이미지** | ❌ 제작사 저작물 |

**서비스 화면에 공연 포스터나 배우 사진을 넣지 않는다.** 텍스트로만 표시한다. 시각적 보완이 필요하면 일러스트 소품(캐스팅보드 등, 배우 얼굴은 실루엣으로 처리)으로 대체한다 — 상담실 덕심 에이전트의 건물 일러스트가 이 방식이다(design.md §2.4a).

### B-3. 커뮤니티·UGC

§11.2에서 이미 범위 밖으로 정했으나, 수익화 관점에서 근거가 강화된다.

- 게시물·댓글은 작성자 개인 저작물이며, **수익 사업에 무단 활용하면 손해배상 대상**이 된다
- 씨야 등 동종 서비스 데이터 수집은 저작권 외에 **부정경쟁방지법상 성과물 무단사용** 소지가 있다
- 무료 사이드 프로젝트일 때보다 수익 서비스일 때 분쟁 가능성이 훨씬 높다

### B-4. LLM API

| 항목 | 조건 |
|---|---|
| 유료 API 상업 이용 | 허용 |
| 출력물 소유권 | 사용자 귀속 (프로바이더별 약관 확인) |
| **무료 티어** | 상업 이용 자체는 가능하나 **입출력이 학습에 사용될 수 있음** |

**§12.1의 무료/유료 경계는 라이선스 문제이기도 하다.** 사용자 상담 데이터를 무료 티어로 보내면 개인정보를 제3자 학습에 제공하는 셈이 되어, 개인정보처리방침 고지 의무와 충돌한다.

### B-5. 서비스명·상표

「이 회차 갈까말까」는 가제다. 확정 전 **특허정보넷 키프리스에서 동일·유사 상표를 검색**한다. 수익화 후에 상표 분쟁이 나면 서비스명 변경 비용이 크다.

도메인과 SNS 계정도 함께 확보한다.

### B-6. 수익화 시 필수 정비

| 항목 | 시점 |
|---|---|
| 이용약관 | 서비스 오픈 시 |
| 개인정보처리방침 | 서비스 오픈 시 (회원가입 있으므로 필수) |
| **상담 데이터 익명화 활용 동의** | 서비스 오픈 시 (§10.3) |
| 만 14세 미만 가입 처리 | 서비스 오픈 시 — **청소년 할인 사용자층 존재(§1.5)** |
| 유료 결제 도입 시 | 전자상거래법상 통신판매업 신고, 청약철회 규정 |
| 광고 도입 시 | 표시광고법상 광고 표시 의무 |

**만 14세 미만 처리는 특히 주의한다.** 케이스 스터디에서 청소년 할인 사용자가 확인되었으며, 개인정보보호법상 만 14세 미만은 법정대리인 동의가 필요하다. 가입 시 연령 확인 절차를 두거나, 만 14세 미만 가입을 제한한다.

### B-7. 디자인 에셋

`design.md` §10 참조. 요약하면 폰트는 SIL OFL 계열만 사용하고, AI 생성 픽셀 아트는 **도트 툴에서 손으로 수정한 최종본**을 사용해 저작권 보호 가능성을 확보한다.

---

## 부록 A. 리뷰 체크리스트

### 원칙 준수
- [ ] 어떤 관점 에이전트도 "가지 마라"/"가라"를 말하지 않는가
- [ ] 화면에 표시한 정보를 시스템이 다시 계산하고 있지 않은가
- [ ] 에이전트 간 통신이 시야→비용 외에 추가되지 않았는가
  - [ ] 추가 이유가 "원본 사실"이면 → 공통 번들로 해결
  - [ ] 추가 이유가 "축 상관"이면 → `primary_drivers`로 해결
- [ ] `primary_drivers`가 열거형을 벗어나지 않았는가
- [ ] 다른 축의 이점을 자기 점수에 가산하고 있지 않은가
- [ ] 반대로, 자기 축에 속한 것을 스스로 깎고 있지 않은가
- [ ] **검증되지 않은 정보로 판단하고 있지 않은가** (원칙 6)
- [ ] 시스템이 알 수 없는 것을 판단하려 하고 있지 않은가

### 계산 규칙
- [ ] 이벤트 점수가 끌림도 × 희소성 승수 구조인가
- [ ] `irreplaceable`이 끌림도 3 이상 + 확정형에서만 켜지는가
- [ ] 시야가 방향 우열을 판정하고 있지 않은가
- [ ] `VALUE` 선호 시 등급 내 상대 위치로 해석하는가
- [ ] 오글이 거리만 보정하고 각도는 그대로 두는가
- [ ] `NO_TRANSFER`를 손실 금액 무관하게 관람 쪽 가중하고 있지 않은가
- [ ] 재관람 혜택 회차 `가지마라` 시 나눔을 검토하는가
- [ ] 컨디션이 부담 3개 이상 누적을 감지하는가
- [ ] 역접 뒤 완화 표현을 대비로 해석하는가

### 데이터
- [ ] 상담 시점에 크롤링이 발생하지 않는가 (DB 조회만)
- [ ] 사용자 데이터가 무료 티어 API로 전송되지 않는가
- [ ] 좌석 등급·정가·할인율을 사용자에게 묻고 있지 않은가
- [ ] 시즌(`season_id`) 단위로 데이터를 조회하는가
- [ ] 나눔 회차의 자리 만족도를 수집하고 있지 않은가
- [ ] 커뮤니티 텍스트가 DB에 저장되거나 노출되지 않는가
- [ ] 애배력 공유 층에 개인정보가 섞이지 않았는가

### 출력
- [ ] 화면·UI 사양이 이 문서에 섞여 들어오지 않았는가
- [ ] 회색 푯말이 `detail`을 생략하는가

### 라이선스 (부록 B)
- [ ] 좌석배치도 **이미지**를 복제하고 있지 않은가 (정보만 추출)
- [ ] 공연 포스터·배우 사진을 서비스에 표시하고 있지 않은가
- [ ] 수집 범위가 서비스에 필요한 작품·극장으로 한정되는가
- [ ] 예매처 `robots.txt`·이용약관을 확인했는가
- [ ] 요청 간격 제한과 User-Agent 명시가 되어 있는가
- [ ] 만 14세 미만 가입 처리 방침이 있는가
- [ ] 서비스명 상표 검색을 마쳤는가
