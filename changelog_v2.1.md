# 변경사항 v2.0 → v2.1

> `prd.md`(v2.0)는 그대로 두고, 이 문서의 변경분만 구현에 반영한다.
> 구현이 안정된 뒤 PRD에 통합할 것.

**변경 배경**

1. 비용 축이 절대 할인율로 판단해 소극장에서 오판이 발생했다. 소극장은 할인이 다양해 30%가 아쉬운 수준이고, 대극장은 20%가 최대치인데 같은 30%가 정반대 의미가 된다.
2. 크롤링 대상이 과도해 개발 단계에서 아무것도 수집되지 않았다.

---

## 1. 비용 축 — 절대 할인율 → 할인 사다리

### 1.1 핵심 전환

```
변경 전: 정가 대비 몇 % 할인받았는가
변경 후: 이 사람이 받을 수 있었던 최선 대비 어느 위치인가
```

**대극장/소극장 분류는 하지 않는다.** 사다리 길이가 극장 성격을 이미 담고 있어 자동으로 조절된다.

### 1.2 할인 사다리

개막 시 예매처 공연 페이지에서 할인 목록을 수집하고 타입을 붙인다.

```json
"discounts": [
  { "name": "조기예매 할인", "rate": 30, "type": "STANDING" },
  { "name": "문화가있는날",  "rate": 20, "type": "STANDING" },
  { "name": "청소년 할인",   "rate": 50, "type": "ELIGIBILITY" },
  { "name": "재관람 할인",   "rate": 40, "type": "LOYALTY" }
]
```

| type | 정의 | 기준선 |
|---|---|---|
| `STANDING` | 상시, 누구나 (조예할·문화가있는날·마티네) | **항상 포함** |
| `ELIGIBILITY` | 자격 보유자 (청소년·대학생·경로·장애인·국가유공자) | 해당 권종 선택 시 포함 |
| `LOYALTY` | 재관람자 전용 (재관람 할인권·도장·쿠폰팩) | **자첫이면 제외** |

**`LIMITED` 타입은 두지 않는다.** 타임세일·쿠폰팩 같은 기간 한정 할인은 개막 시점 목록에 없을 수 있으며, 이는 §1.5의 "목록에 없는 할인"으로 처리한다.

> **개정 (2026-08)**: 할인 항목에 선택 필드 `applies_to`(`ALL`/`MATINEE`/`EVENING`) 추가.
> 마티네 전용 할인은 낮공/밤공을 이미 알므로, 밤공 상담에서는 기준선(`baselineRate`)에서 뺀다.
> 회차를 모르면 종전대로 포함(보수적). 예매일을 안 받으므로 조기예매 등 기간 조건은 여전히 필터하지 않고,
> 관리자 도구가 원문을 `note` 로만 저장한다. `type` 은 3종 그대로.

**타입 분류는 크롤링 파싱 단계에서 LLM이 할인명·설명을 보고 수행한다.** 규칙 기반은 새 할인명마다 코드 수정이 필요해 유지되지 않는다. 애매하면 `STANDING`으로 둔다(기준선이 높아져 판정이 보수적이 되므로 안전).

### 1.3 기준선

```
기준선 = max(
  STANDING 최댓값,
  사용자가 선택한 ELIGIBILITY 권종의 값,
  자첫이 아닐 경우 LOYALTY 최댓값
)
```

같은 공연에서도 사용자마다 다르다.

| 사용자 | 기준선 |
|---|---|
| 자첫 성인 | 30% (조예할) |
| 회전 중 성인 | 40% (재관람) |
| 청소년 | 50% (청소년 할인) |

**자첫 여부는 R-6에 이미 있으므로 추가 질문이 없다.**

### 1.4 밴드 판정

| 기준선 대비 | axis_score |
|---|---|
| -5%p 이내 (초과 포함) | +2 |
| -5 ~ -15%p | +1 |
| -15 ~ -25%p | 0 |
| -25%p 이상 | -1 |
| 정가 (할인 0%) | -2 |

구간이 10%p 단위이므로 예매수수료·포인트 등 소액 차이는 판정을 바꾸지 않는다.

### 1.5 입력 — 역추적 폐기

**할인율을 역산해 권종을 추정하는 방식은 폐기한다.** 5% 할인이 두 개 있거나, 포인트·자체 쿠폰이 섞이면 추정이 깨진다.

**사용자가 권종을 직접 고른다.**

```
▸ 어떤 할인을 받으셨나요?
  ○ 없음 (정가)
  ○ 조기예매 할인 30%        ← 크롤링한 목록을 그대로 표시
  ○ 청소년 할인 50%
  ○ 재관람 할인 40%
  ○ 문화가있는날 20%
  ○ 목록에 없는 할인          ← 타임세일·쿠폰팩·이벤트 할인 등

▸ 결제한 금액  [ 46,500 ] 원

▸ 증빙 가능하신가요?
     ↑ ELIGIBILITY·LOYALTY 권종 또는 "목록에 없는 할인" 선택 시에만 노출
  ○ 증빙 필요 없어요          ← 타임세일·포인트 등
  ○ 가능해요                 ← 실물·서류 보유
  ○ 불가해요
       ↓
       ○ 정가로 추가결제
       ○ 다른 권종으로 변경 → [드롭다운]
```

**추가 할인 입력칸은 두지 않는다.** 결제 금액과 권종 적용가의 차액으로 자동 산출된다.

```
R석 70,000 × (1 - 0.30) = 49,000   ← 권종 적용가
실제 결제                 46,500
차액                       2,500   ← 예매수수료 면제·포인트·자체 쿠폰 등
```

**차액의 정체를 추정하지 않는다.** 증빙과 무관한 항목들이므로 실제 부담액 계산에만 반영한다.

차액이 음수여도(권종가보다 더 냄) 정상이다. 예매수수료가 붙은 경우다. 다만 **차이가 20% 이상이면 "금액이나 할인 종류를 다시 확인해보세요" 확인 문구**를 띄운다.

**"목록에 없는 할인"** 선택 시 결제 금액에서 할인율을 역산한다. 이 경우에만 역산을 쓰며, 어느 할인인지 알 필요가 없으므로 겹침 문제가 발생하지 않는다.

### 1.6 증빙 불가 시 추가결제

```
증빙 불가 + discount_proof_policy = FULL_PRICE
  → 추가결제 = 정가 - 결제액
  → 실제 부담 = 정가

증빙 불가 + GRADE_CHANGE
  → 대체 권종 = 사용자가 선택 (드롭다운)
  → 추가결제 = 대체 권종 적용가 - 결제액
```

#### 계산 예 (해몽가 R석 70,000, 청소년 50%, 35,000 결제)

```
증빙 가능             → 부담 35,000 (50%) → 기준선 50% 대비 0%p  → +2
증빙 불가 + FULL_PRICE → 추가 35,000 → 부담 70,000 (0%)  → -2
증빙 불가 + 조예할 변경 → 추가 14,000 → 부담 49,000 (30%) → 기준선 30% → +2
```

### 1.7 클라이언트 즉시 계산

전 과정이 결정론적이므로 LLM 호출 없이 입력 즉시 계산된다.

```
공연 제목 선택 → 시즌 데이터 프리로드 (정가·등급매핑·할인사다리·정책)
좌석 입력      → 등급 판정 → 정가 확정          [즉시]
권종 선택      → 권종 적용가 계산               [즉시]
금액 입력      → 차액·할인율 계산               [즉시]
증빙 선택      → 추가결제액·실제 부담액 갱신     [즉시]
```

**화면에는 사실만 표시하고 판정하지 않는다.**

```
⭕ R석 정가 70,000원 · 33.6% 할인
❌ 최대 할인 받으셨어요!
```

판정은 비용 에이전트가 메인 페이지에서 푯말로 할 말이다. 입력 화면에서 미리 말하면 푯말의 가치가 사라지고, 5개 축을 함께 보라는 구조가 깨진다. 시야 에이전트의 "위치는 사실로 서술하되 우열은 판정하지 않는다"와 같은 규칙이다.

**계산 결과는 확정값으로 번들에 담아 전달한다.**

```json
"payment": {
  "total_paid": 46500,
  "list_price": 70000,
  "grade": "R",
  "selected_discount": { "name": "조기예매 할인", "rate": 30, "type": "STANDING" },
  "proof_status": "NOT_REQUIRED | AVAILABLE | UNAVAILABLE",
  "surcharge": 0,
  "actual_burden": 46500,
  "discount_rate": 33.6,
  "baseline_rate": 30.0,
  "gap": 3.6,
  "band": 2
}
```

**LLM이 산수를 하지 않는다.** `band`까지 코드에서 확정해 넘기고, 에이전트는 푯말 문구와 설명만 작성한다.

### 1.8 알려진 오차 — 감수함

기간 한정 할인(타임세일·쿠폰팩)이 개막 시 목록에 없으면 기준선이 실제보다 낮게 잡힌다.

```
사다리(회전 중): 재관람 40% → 기준선 40%
실제로는 쿠폰팩 50%도 존재 → 진짜 기준선은 50%
→ 40% 받은 사용자가 실제보다 한 밴드 후하게 평가됨
```

**감수한다.** 자첫에게는 `LOYALTY`가 제외되므로 무관하고, 회전 사용자에게만 밴드 하나 차이다. **후하게 평가되는 방향이라 억울한 감점이 발생하지 않는다.** 이를 잡기 위해 정기 크롤링 배치를 유지하는 것은 비용 대비 효과가 낮다.

---

## 2. 입력 변경 요약

### 2.1 R-4 티켓값 — 직접/양도 분기 삭제

**변경 전**
```
① 직접 예매 → 실지불액
② 양도받음   → 실지불액 + 할인권 보유 여부 + 정가 추가결제/권종변경 차액
```

**변경 후** — §1.5 참조. 할인 권종 선택 + 결제 금액 + (조건부) 증빙 3택

**분기를 없앤 이유:** 직접 예매한 표를 남에게 양도할 때도 권종 증빙 문제가 발생한다. 분기로 나누면 "내 표"라고 답한 사용자에게 증빙 질문이 뜨지 않는다.

**"권종 증빙"** 표현을 쓴다. 자격 권종뿐 아니라 재관람 할인권·쿠폰팩 실물 소지 여부까지 포괄한다.

### 2.2 R-5 — 배우 이름 입력 삭제

```
오늘 본진·애배 출연?
  ○ 출연함   ○ 출연 안 함   ○ 해당 없음
```

이름을 받아도 판단에 쓰이지 않는다. 페어 조합 입력 안내 문구도 삭제한다.

**부수 효과:** 시스템 전체에서 배우 이름이 사라진다. 프로필 애배 목록은 온보딩 폐지로 이미 제거되었으므로 어디에도 배우 이름을 저장하지 않는다. PRD 부록 B의 배우 초상·퍼블리시티권 항목이 무의미해진다.

### 2.3 S-1 기타란 — 라벨 변경, 기본 펼침

**변경 전**
```
▸ 그 밖에 고민되는 부분이 있나요? (선택)
```

**변경 후**
```
▸ 이 회차만의 사정이 있나요?

   컨디션, 전날·당일·다음날 일정, 막차 시간,
   부대비용(KTX·숙소·촬영 장비 등),
   페어막·첫공·막공 등
   앞에서 못 담은 이야기를 적어주세요
```

- 기본 상태를 **접힘 → 펼침**
- "(선택)" 표기 삭제 (비워도 넘어가지만 선택지로 읽히지 않게)

**근거:** 케이스 스터디 6건 중 기타란이 결론을 뒤집은 것이 2건(CASE 5 거부권, CASE 6 나눔). 앞의 항목을 다 채우면 "더 없다"는 심리가 생겨 넘어가는데, 실제로는 가장 결정적인 정보가 들어오는 칸이다.

---

## 3. 덕심 축 — 남은 애배 회차 → 시즌 위치

### 3.1 삭제

**`remaining_favorite_sessions`(남은 시즌 내 애배 출연 회차 수)를 제거한다.**

계산하려면 시즌 전체 캐스팅보드가 필요한데, 배우 이름을 받지 않으므로 특정 배우의 남은 회차를 매칭할 수 없다.

### 3.2 대체 — 시즌 위치

폐막일과 오늘 날짜만으로 계산된다. 크롤링 실패와 무관하다.

```
progress = (오늘 - 개막일) / (폐막일 - 개막일)
```

| 구간 | 서술 방향 |
|---|---|
| 초반 (~30%) | **무게를 낮춤.** 앞으로 더 좋은 조건이 생길 수 있음 |
| 중반 (30~70%) | 언급하지 않음 |
| 후반 (70~90%) | 남은 기간이 줄어듦 |
| 막바지 (90%~) | 폐막 임박 |

**시즌 초반에 무게를 낮추는 서술이 새로 추가된 개념이다.** 기존 덕심은 항상 가산 방향으로만 말했으나, 초반이면 오늘의 대체불가성이 실제로 낮다.

```
시즌 초반
> 애배가 나오는 회차입니다. 다만 폐막까지 두 달 넘게 남아 있어
> 앞으로 더 좋은 자리나 조건으로 볼 기회가 생길 수 있습니다.

시즌 막바지
> 애배가 나오는 회차이고 폐막까지 2주가 채 남지 않았습니다.
> 이번 시즌에 남은 기회가 많지 않습니다.
```

애배 출연 자체는 여전히 +2를 만들고, 시즌 위치가 그 무게를 조절한다.

### 3.3 페어 — 언급 금지

**시스템이 페어 조합 희소성을 판단하지 않는다.** 조합 경우의 수가 많아 산술적으로는 모두 희소하게 나오고, 팬덤이 실제로 특별하게 치는 조합인지는 데이터로 판별할 수 없다.

**기타란에 언급이 있을 때만 덕심이 읽고 반영한다.** 첫공·막공도 동일하게 처리한다.

### 3.4 덕심 입력 최종

| 요소 | 출처 |
|---|---|
| 애배 출연 여부 | 사용자 입력 (R-5) |
| 자첫 여부 | 사용자 입력 (R-6) |
| 이 작품 관람 횟수 | 관람 이력 |
| 시즌 위치 | 개막일·폐막일 계산 |
| 촬영 허용 | 사용자 입력 (이벤트) |
| 페어막·첫공·막공 | **기타란 조건부** |
| ~~페어 조합 희소성~~ | 삭제 |
| ~~남은 애배 회차~~ | 삭제 |
| ~~배역 비중·분량~~ | 삭제됨 (v2.0, CASE 2) |

---

## 4. 데이터 수집 — 정기 배치 전면 폐기

### 4.1 최종 수집 대상

| 시점 | 대상 | 방식 |
|---|---|---|
| **극장 신규 등록** | 열·번호 체계, **통로 위치**, 단차, 층 구조, 무대 거리 | **수동 1회** |
| **개막 시 1회** | 제목, 극장, 공연기간, 러닝타임, 등급별 정가, **좌석 등급 매핑**, **할인 목록**, 시야제한석, 취소 수수료 규정, **`discount_proof_policy`** | 크롤링 |

**정기 배치가 없다.** 신규 시즌이 등록될 때만 크롤러가 돈다. 스케줄러가 불필요하다.

**할인 목록을 일 1회로 잡았다가 철회한 이유:** 기준선을 만드는 `STANDING`·`ELIGIBILITY`·`LOYALTY`는 시즌 내내 불변이다. 매일 긁어서 얻는 것은 "지금 타임세일 중"이라는 사실 하나인데, 이는 기준선에 들어가지 않고 사용자가 이미 표를 산 상태라 판단에 영향이 없다.

### 4.2 폐기된 수집 대상

| 대상 | 사유 |
|---|---|
| **캐스팅보드 / 스케줄표** | 배우 이름을 받지 않아 매칭 불가. 화면 표시 용도도 불필요 |
| **이벤트 정보** | v2.0에서 폐기 (사후 검색 성공률 0/6) |
| 제작사 SNS 크롤러 | 이벤트 폐기로 불필요 |
| LLM 이벤트 파서 | 동일 |
| 시즌 매칭 로직 | 동일 |
| 정기 배치 스케줄러 | §4.1 |

**이벤트 크롤링에 대한 단서:** v2.0의 폐기 근거였던 "성공률 0/6"은 **과거 회차를 사후에 검색한 결과**였다. 이벤트 공지는 공연 전에 올라왔다가 지나면 묻히므로, 실시간 수집이었다면 결과가 달랐을 수 있다. 다만 사용자 직접 입력이 주 경로이고 크롤링은 "발견" 편의 기능에 불과하므로, **Phase 2 이후 재검토 대상**으로만 남긴다. 이를 위해 배치를 새로 만들 값어치는 현재 없다.

### 4.3 좌석 데이터 2층 분리

극장의 물리 구조와 시즌의 좌석 운용은 다른 데이터다.

```json
// theaters/yes24_stage_1.json — 극장 신규 등록 시 수동 1회
{
  "floors": [{
    "floor": 1,
    "rows": [
      { "row": "A", "seats": [1, 16], "aisles_after": [4, 12] },
      { "row": "B", "seats": [1, 16], "aisles_after": [4, 12] }
    ]
  }]
}

// seasons/{season_id}/seat_grades.json — 개막 시 크롤링
{
  "grade_map": [
    { "grade": "R", "rows": ["A","B","C","D","E","F","G"], "seats": [[5,12]] },
    { "grade": "S", "rows": ["A","B","C","D","E","F","G"], "seats": [[1,4],[13,16]] }
  ],
  "op_seats": [],
  "restricted": [{ "row": "가", "seats": [1,2,3] }],
  "unsold": [],
  "seat_map_override": null
}
```

`aisles_after`만 있으면 통로석 판정, 중앙 거리, 블럭 구분이 모두 계산된다.

**극장 데이터는 시즌·작품이 바뀌어도 재사용된다.** 20개 내외로 유한하므로 크롤러를 붙이는 것이 오히려 과잉이다. 반면 등급 매핑은 무대 세트에 따라 시즌마다 바뀌므로 크롤링 대상이다.

**세트가 객석을 침범하는 예외**는 `seat_map_override`로 처리한다.

```json
"seat_map_override": {
  "note": "돌출무대로 1층 가~다열 미사용",
  "removed_rows": ["가", "나", "다"]
}
```

**클라이언트에서 `(row, number) → grade` 조회가 O(1)이 되도록 한 번 인덱싱해둔다.**

### 4.4 신규 필드 — `discount_proof_policy`

권종 증빙 실패 시 처리 방식이 공연마다 다르다. 예매처 공지·유의사항에 명시되어 있으므로 취소 규정과 함께 수집한다.

```json
"discount_proof_policy": "FULL_PRICE" | "GRADE_CHANGE" | "UNKNOWN"
```

| 값 | 처리 |
|---|---|
| `FULL_PRICE` | 정가 차액 강제 |
| `GRADE_CHANGE` | 권종 변경 후 차액. 대체 권종은 사용자가 드롭다운에서 선택 |
| `UNKNOWN` | 양쪽 시나리오 병기, confidence 하향 |

**`NO_ENTRY`(입장 불가)는 두지 않는다.** 장애인·국가유공자 할인도 다른 자격 권종과 동일하게 계산한다. 공지에 양도 불가로 적혀 있어도 실제 현장 운용은 증빙 요구 후 차액 처리인 회색지대이며, 시스템이 규정만 보고 "이 표는 사용 불가"로 단정하면 실제와 어긋난다. **비용 에이전트는 금액만 계산하고 입장 가능 여부는 판단하지 않는다.**

---

## 5. 수집 실패 알림 체계 (신규)

크롤링 실패를 사용자 질문으로 전가하지 않고 운영에서 처리한다.

### 5.1 심각도

| 등급 | 대상 | 영향 | 알림 |
|---|---|---|---|
| **치명** | 등급 매핑, 정가 | 비용 축 판단 불가 | 즉시 |
| **경고** | 할인 목록, `discount_proof_policy` | 사다리 비교 불가 | 일 1회 요약 |
| **정보** | 시야제한석, 취소 규정 | 정확도 저하 | 주 1회 요약 |

정기 배치가 없으므로 **알림은 신규 시즌 등록 시점에만 발생한다.** 그만큼 한 건 한 건이 중요하다. 기본 정보를 못 긁으면 그 작품 상담이 전부 막힌다.

### 5.2 알림 내용

무엇이 실패했는지보다 **어디를 고치면 되는지**가 들어가야 한다.

```
[치명] 등급 매핑 수집 실패
작품: 해몽가 2026 (season_id: 142)
예매처: YES24
원본: https://...
사유: 좌석 선택 페이지 구조 변경 추정 (셀렉터 불일치)

→ 수동 입력: /admin/seasons/142/seats
```

**수동 입력 경로를 반드시 함께 제공한다.** 극장 데이터를 수동으로 채우는 관리자 화면이 어차피 필요하므로, 시즌 데이터도 같은 화면에서 처리한다.

### 5.3 연쇄 실패 묶음

한 예매처가 페이지 구조를 바꾸면 그 예매처의 모든 신규 시즌이 실패한다. **같은 예매처·같은 사유로 3건 이상이면 개별 알림 대신 하나로 묶는다.**

```
[치명] YES24 파서 전면 실패
영향: 12개 시즌
사유: 셀렉터 불일치 (동일)
→ 파서 점검 필요
```

### 5.4 사용자 측 처리

데이터가 불완전한 시즌도 **상담을 막지 않되 정직하게 표시한다.**

```json
{ "axis_score": 0, "confidence": 0.3,
  "missing_info": ["좌석 등급", "할인 목록"],
  "placard": "가격 정보가 부족해요" }
```

**등급 매핑이 없으면 조용히 넘어간다.** 사용자에게 등급을 묻지 않고 화면에 정가를 표시하지 않으며, 할인 권종 선택도 노출하지 않는다. 사용자는 "이 서비스가 지금 모르는 것이 있다"만 알면 되고, 왜 모르는지는 알 필요가 없다.

---

## 6. 스키마 변경

### 6.1 `seasons` 추가

```sql
ALTER TABLE seasons ADD COLUMN discount_proof_policy VARCHAR(20);
```

### 6.2 신규 테이블

```sql
season_discounts (
  season_id FK,
  name, rate, type,        -- STANDING | ELIGIBILITY | LOYALTY
  raw_description,
  crawled_at
)

crawl_failures (
  id PK, season_id FK,
  target,                  -- SEAT_GRADE | PRICE | DISCOUNT | POLICY ...
  severity,                -- CRITICAL | WARNING | INFO
  source_url, reason,
  created_at, resolved_at
)
```

### 6.3 삭제

```sql
DROP TABLE season_castings;
```

### 6.4 `consultations` 조정

```sql
-- 삭제
acquisition, surcharge

-- 추가 / 유지
total_paid              -- 결제한 금액
selected_discount_name  -- 선택한 권종 (null = 정가, 'OTHER' = 목록에 없음)
proof_status            -- NOT_REQUIRED | AVAILABLE | UNAVAILABLE
surcharge_type          -- FULL_PRICE | GRADE_CHANGE | null
alternative_grade_name  -- 권종 변경 시 선택한 대체 권종
```

### 6.5 공통 입력 번들

```json
"payment": {
  "total_paid": 46500,
  "list_price": 70000,
  "grade": "R",
  "selected_discount": { "name": "조기예매 할인", "rate": 30, "type": "STANDING" },
  "proof_status": "NOT_REQUIRED",
  "surcharge": 0,
  "actual_burden": 46500,
  "discount_rate": 33.6,
  "baseline_rate": 30.0,
  "gap": 3.6,
  "band": 2,
  "cancellation_fee": 0,
  "discount_proof_policy": "GRADE_CHANGE"
},
"season_progress": 0.42,
"casting": { "has_favorite_actor": true }
```

`today_cast`, `favorite_actors_today`, `remaining_favorite_sessions` 삭제.

---

## 7. 구현 영향 범위

| 영역 | 작업 |
|---|---|
| **DB 마이그레이션** | `season_discounts`·`crawl_failures` 생성, `season_castings` 삭제, `seasons`·`consultations` 컬럼 조정 |
| **크롤러** | 캐스팅 파서 제거, **스케줄러 제거**, 할인 목록 파서 신규, `discount_proof_policy` 파서 신규, 좌석 등급 매핑 파서 유지 |
| **LLM 파서** | 할인 타입 3분류 프롬프트 신규 (공개 정보이므로 무료 티어 사용 가능) |
| **입력 폼** | R-4 전면 재작성(권종 선택 + 금액 + 조건부 증빙), R-5 이름 입력 제거, 기타란 라벨·기본 펼침 |
| **클라이언트 계산** | 시즌 데이터 프리로드, 등급 인덱싱, 즉시 계산, 사실만 표시 |
| **비용 에이전트** | 사다리 기준선·밴드 판정은 코드에서 확정. 프롬프트는 해석·서술만 담당하도록 전면 수정 |
| **덕심 에이전트** | 시즌 위치 계산, 초반 무게 하향 서술, 페어 언급 금지 |
| **관리자** | 극장·시즌 수동 입력 화면, 실패 알림 대시보드 |
| **알림** | 심각도 분류, 연쇄 실패 묶음 |

---

## 8. 개발 단계 운영

**개발 중에는 크롤링 대신 시드 데이터를 사용한다.**

```
seed/
  theaters/
    yes24_stage_1.json        # 수동 작성
    ...
  seasons/
    haemonga_2026.json        # 기본정보 + 정가 + 할인사다리 + 등급매핑 + 정책
    ...
```

배포 전 크롤러로 대체한다. **시드 데이터의 스키마는 크롤러 출력 스키마와 동일해야 한다.** 그래야 교체 시 코드 수정이 없다.

---

## 9. 미해결 / 검증 필요

| 항목 | 내용 |
|---|---|
| 밴드 구간 수치 | ±5/15/25%p는 초안. 시드 데이터로 여러 공연을 돌려보고 조정 |
| 시즌 위치 구간 | 30%/70%/90% 경계값 미검증 |
| 등급 매핑 크롤링 성공률 | 좌석 선택 화면이 JS 렌더링이면 정적 파싱 불가. 예매처별 확인 필요 |
| `discount_proof_policy` 명시율 | 공지에 실제로 얼마나 적혀 있는지 미확인. `UNKNOWN` 비율이 높으면 필드 재검토 |
| 할인 목록 파싱 정확도 | 할인명·율·조건이 한 덩어리 텍스트로 있는 경우 분리 정확도 |
| 이벤트 크롤링 재도입 | Phase 2 이후. 실시간 수집이면 결과가 다를 수 있음 |
